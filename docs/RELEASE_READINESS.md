# 正式公開の到達点（2026-09-06 時点）

- 目的：**いまはテスト公開版であり、正式公開ではない**（D-09）。正式公開に何が要るかが `APP_SPEC` §1.1・`HANDOFF`・各発注書へ散っているため、1枚へ集める。
- 作成：2026-09-06、Opus 5。**読み取り調査のみ。コードは1行も変えていない。**
- 使い方：正式公開の可否はこの表で判断する。**「画面が動く」ことは正式公開の条件ではない。**
- 注意：この表は 2026-09-06 の作業ツリー（058公開済み＋059実装済み＋Codex作業中）を読んだ結果である。**Codex が同じツリーで追加改善を実装中のため、UIに関わる行は着地後に取り直すこと。**

## 1. `APP_SPEC` §1.1 が定める初回公開（MVP）の範囲

| # | 要件 | 状態 | 根拠・残り |
|---|---|---|---|
| 1 | 100首の本文・作者・歴史的仮名遣い・現代仮名遣いの閲覧 | **満たす** | 公開版で実測。`check:overflow` 閲覧1200件が違反0 |
| 2 | 範囲指定（URL・画面）、**5つの学習入口**、順序・ランダム出題 | **未達** | Home から到達できるのは `learn`・`exam`・`view`＋作者閲覧。**`quick` は到達経路が無い**（発注058 §2 Q2）。`review` は結果画面からのみ |
| 3 | 穴埋め問題、**作者問題**、結果表示、任意の再確認 | **未達（意図的）** | **作者問題300問はどこからも出題されない。** 058 R1 で初回公開から外す裁定。習熟度の配点も本文100%へ寄せた。**正式公開では戻す必要がある**（戻し方は `APP_SPEC` §8 の但し書き） |
| 4 | 端末内保存、JSONの書き出し・読み込み・重複排除マージ | **達成（2026-09-06 配線）** | 「これまでの記録」に持ち出しの節を追加。**記録の削除だけは未配線**（下記 §2.4） |
| 5 | 習熟度イベントの計算と本人向けの表示 | **満たす** | ただし №3 のとおり配点が暫定 |
| 6 | **学年区分を含む匿名統計**（案内文の表示）と問題報告の受付 | **未達** | **統計は1件も送っていない。**`packages/hyakunin/src` は telemetry を一度も参照していない。059 F-3 で案内と学年選択も非表示にした。問題報告の受付状況は未確認 |
| 7 | 公開用静的Webビルドと、管理者だけが確認する非公開の確認手段 | **一部** | ビルドと Pages 公開は動作。非公開の確認手段は未確認 |

**正式公開を名乗るには、少なくとも №2・№3・№6 の3つが埋まる必要がある。** いずれも「直し忘れ」ではなく、テスト公開のために意図的に外した／未着手のものである。

## 2. 公開の門そのものに残っている欠陥

| # | 事項 | 状態 |
|---|---|---|
| G-1 | **公開版の GitHub Actions `CI` が赤**（初回公開の次から3回連続） | 059 F-1 で実装済みと報告あり。**未検収。公開後に CI 緑を実測すること。`Deploy Pages` の緑で代用しない** |
| G-2 | 公開手順が `Deploy Pages` しか見ていなかった | 059 で `MIGRATION_LOG` §7 へ CI 確認を追記と報告。**未検収** |
| G-3 | `review/readings.yaml` は**100首とも `pending`**。承認経路が一度も動いていない | 未着手。既定で `confirmed` として出ている。正式公開の前に運用を決める |
| G-4 | `Session.tsx` が歌の `reading.status` を判定へ渡していない | 未着手。現在は全首 `confirmed` のため実害なし。**将来「保留」が出ても届かない** |

## 2.4 MVP④ の内訳（2026-09-06 実測）

`packages/shared/src/storage/` の輸出21個のうち、`packages/hyakunin/src` が参照しているのは**3個だけ**である。

| 輸出 | 使用状況 |
|---|---|
| `openDatabase`・`readFallback`・`writeFallback` | IndexedDB ポートが使用。**保存自体は動いている** |
| `createExport`・`makeExportDocument`・`serializeExport`・`summarizeExport` | **未参照** |
| `parseImport`・`applyImport` | **未参照** |
| `mergeEvents`・`mergeSessions`・`mergeReports` | **未参照** |
| `resetRecords`・`previewReset`・`makeResetConfirmation` | **未参照**（記録の削除も導線が無い） |

`tests/unit/storage/` は35件すべて緑であり、**関数そのものは動く。欠けているのは画面からの導線だけである。**

**作者問題と同じ型の見落としである**（生成・実装・試験はあるが、UIから到達できない）。`npm test` は緑のままなので、試験だけでは気づけない。**「実装がある」と「使える」を分けて数えること。**

**検証方法**（再実行できる形で残す）：

```
grep -hoE "export (async )?function [A-Za-z0-9_]+|export const [A-Za-z0-9_]+" packages/shared/src/storage/*.ts   | sed -E 's/export (async )?(function|const) //' | sort -u
# 出た名前を1つずつ packages/hyakunin/src で grep -rlw する
```

**推測した関数名で grep して空だったことを未配線の証拠にしないこと。** 最初にそれをやり、輸出名を確かめて引き直した。

## 2.5 MVP④ の配線（2026-09-06、Opus 5）

**画面**：`RecordTransfer.tsx` を新設し、「これまでの記録」の**着手した歌の直後**へ置いた。最初は全100首の一覧の下に置いたが、**100行スクロールしないと届かない**ため上へ移した（320px の実機相当で気づいた）。記録が空のときも出す——別の端末から持ち込む人には、そこが唯一の入口だからである。

**取り込みは2段階**にした（`APP_SPEC` §9「読み込み前に件数を示して確認する」）。ファイルを選ぶと件数・対象製品・書き出し版を表示するだけで、**下見の段階では1件も書かない。**「この内容で読み込む」を押して初めて `applyImport` が走る。結果は「◯件を追加し、◯件は同じ記録なので重ねませんでした」と出す。

**ポート**：`ApplicationPort` に `exportRecords`／`previewImport`／`commitImport` を足した。`packages/shared/package.json` の輸出マップに **`./storage/export`・`./storage/import` が無く**、そもそも import できない状態だったので追加した（未配線の裏付けでもある）。

**任意型にした代償と、その埋め合わせ**：3つの口は `Partial<TransferPort>` で任意にしてある（`Home.tsx` の簡易ポートに実装を強いないため）。**本番のポートから落ちても型は通り、画面は黙って機能ごと消える。** そこで `tests/screen/history-transfer.test.tsx` に「本番のポートは3つの口をすべて持つ」を名指しで置いた。破壊試験で `exportRecords` を落とすと、**この1件だけが赤くなる**ことを確認した。

**320px・文字200% の実測**：違反0、測定対象6件、ページのあふれ0。途中で2つ直した——(1) ネイティブのファイル入力が固有幅で縮まず器を548pxへ広げていた。入力を `sr-only` で隠し、ラベル自体をボタンとして見せる形にした（入力はラベルの子なので押せば選択画面が開き、支援技術にはそのまま見える）。(2) ボタンの文字が器を2px超えていたので折り返しを許した。
**一度、測定対象0件で「違反なし」が出た。** HMR で画面が戻り、`.record-transfer` が存在しないまま数えていた。**測定件数を必ず併記していたので気づけた。**

**未配線のまま残るもの**：記録の削除（`resetRecords`・`previewReset`・`makeResetConfirmation`）。書き出し・読み込みとは別の裁定（消してよいかの確認手順）が要るため、本作業には含めていない。

**この節は `check:overflow` の走査対象外である。** 既存5群に「これまでの記録」画面が無いため、上の実測はブラウザーでの手動測定であって、以後の回帰では守られない。走査群の追加は `tools/overflow-check/index.ts` を触るため、Codex の059作業と衝突する。**次の担当が引き取ること。**

## 3. 表示・体裁で残っているもの

| # | 事項 | 状態 |
|---|---|---|
| D-1 | フッタが「テスト公開版 0.1.0・正式公開ではありません」 | **正式公開時に必ず変える。** 変え忘れると、正式公開なのに否認の文言が出る |
| D-2 | 習熟度一覧（100首）の見せ方 | 058 R4 で見送り。**別途検討** |
| D-3 | 出題画面が文字200%・320/375/414px で切れる | 059 F-2 で実装済みと報告あり。**未検収** |
| D-4 | ⑤「5句後に空欄のない入力画面・行き止まり」 | 実機で**再現しなかった**が、原因を特定して直したわけではない |

## 4. 次にやると効くこと（起草者の見立て）

1. **№6（統計）を決める。** 送るなら配線と案内の復帰、送らないなら `APP_SPEC` §1.1 から外す裁定が要る。**「未実装のまま正式公開」は選べない。** 仕様が公開を名乗る条件として挙げているためである。
2. **№3（作者問題）を決める。** 戻すなら習熟度の配点を 0.8/0.2 へ戻し、`planQuestions` の穴埋め絞り込みも戻す（両方を同時に戻さないと80%上限が復活する）。
3. **№2（5つの入口）** は `quick` の導線を足すだけで済む可能性がある。**着手前に、5入口が本当に要るのかを確認すること**——Home の現在の設計は意図的に絞られている。
4. G-1 の検収。**公開の門が機能していない状態で正式公開へ進まない。**

## 5. この文書について

- **判断材料であって裁定ではない。** 各行の「未達」は、直すべきという意味ではなく、**正式公開を名乗るなら仕様側を直すか実装側を直すかを決める必要がある**という意味である。
- 状態が変わったら更新する。**古いまま残すと、埋まっていない要件を埋まったと誤読させる。**
