# ADR-0004: 2 プロダクト構成（裁定 D-06）と習熟度蓄積単位（裁定 D-01）

- 状態: 採択
- 日付: 2026-08-30
- 決定者: 依頼者（裁定）
- 関連: `docs/IMPLEMENTATION_PLAN.md` §4.4、§6.3、`docs/HANDOFF.md` §4.3、`docs/superpowers/specs/2026-08-30-歴史的仮名遣い確認ツール-design.md` §6・§8、`CONSTITUTION.md` §1・§6

## 文脈

2026-08-30、依頼者の指示によりコミット `bf6f7c2` で並行プロダクト「歴史的仮名遣い確認ツール」の設計書が追加された。同設計書は冒頭で「古典学習帳（百人一首アプリ）とは同一リポジトリ内の並行プロダクトとする。`CONSTITUTION.md`、`docs/DESIGN_SYSTEM.md`、習熟度モデル、保存スキーマを共有し、公開単位とデータだけを分ける」と宣言している。

`docs/IMPLEMENTATION_PLAN.md` は単一プロダクト前提で書かれており（§4.1・§4.2 の `app/` 単一ツリー構成）、この前提が崩れた。`docs/HANDOFF.md` §4.3 はこれを「裁定 D-06（2 プロダクトのディレクトリ構成・共有層・公開単位・保存スキーマ共有）」が必要な論点として整理し、実装計画 §4.4 に推奨案を追加した。

同時に、共有スキーマの設計は「習熟度の蓄積単位（`itemKey`）」という別の未決事項に依存していた。先方設計書は §6 で「習熟度は語ID × 出題形式を単位とする内部指標」と書き、§8 では `events` の `itemKey` を「百人一首アプリが `poemId × skill`、本ツールが `wordId × rule`」と定義しており、蓄積単位の定義が §6 と §8 で食い違っていた。`wordId × rule` を蓄積単位にすると、複数規則が絡む語（例: てふ）で 1 回の正答が複数キーへ加点され、`docs/APP_SPEC.md` §8 の上限表と「同一回の再回答は加点半分」の算術が壊れる。これは実装計画 §6.3 が「裁定 D-01 が必要」として先に指摘していた論点と同一線上にあり、`docs/HANDOFF.md` §4.3 も「D-06 と同時に正式裁定してよい」としていた。

本 ADR は、2026-08-30 に依頼者が下した裁定 D-06（実装計画 §4.4 の推奨案を補正 3 点つきで承認）と裁定 D-01（`itemKey` の定義）を記録する。

## 決定

### D-06: 2 プロダクト構成（実装計画 §4.4 の推奨案を補正 3 点つきで承認）

実装計画 §4.4 の骨格をそのまま承認する。

- 共有層 `packages/shared` を置き、公開単位を `packages/hyakunin`（古典学習帳／百人一首アプリ）と `packages/kanazukai`（歴史的仮名遣い確認ツール）に分ける。
- 公開リポジトリは **1 つ**とし、GitHub Pages の別パス（`/hyakunin/`、`/kanazukai/`）で 2 つを配信する。
- 実装計画 §4.4「共有スキーマで決めておくこと」の表にある 8 項目（`product` 判別子の追加、IndexedDB は 1 DB に統一、書き出し形式の改訂要否、初期化のプロダクト単位選択、無作為な利用番号の共有、統計 payload の product 別集計、習熟度係数の共有、問題報告への `product` 付与）を採用する。ただし 1 項目目（`itemKey` の定義）は下記 D-01 の結論に差し替える（補正 2）。
- `packages/shared` は npm workspace のローカルパッケージとし、公開ビルド時に各公開単位（`packages/hyakunin`、`packages/kanazukai`）へバンドルする。ランタイム依存として配布しない。

**補正 1（無作為な利用番号の共有はプライバシー境界の判断である）**

実装計画 §4.4 の表は、利用番号の共有を「同一ブラウザ保存領域で番号を 2 つ持つ理由がない」という利便判断として書いている。これは正確な位置づけではない。**利用番号の共有は、プライバシー境界の判断として決定し、記録する。**

- 共有した結果として何が起きるか: 統計文書は「この利用番号の利用者は、2 プロダクトの両方を使っている」という情報を持つことになる。これは単なる集計の一項目ではなく、1 人の利用者の行動を 2 つの学習ツールにまたがって突き合わせられる情報である。
- **これを明示的な受容事項として記録する。**
- 受容する理由: 番号は同一ブラウザの同一保存領域（例: 同一オリジンの IndexedDB／LocalStorage）に置かれる。プロダクトごとに番号を分けても、同じ保存領域にある以上、技術的な分離の実態はない。番号を分けることは「分離している」という誤った説明を利用者・管理者双方に与えるだけであり、実態のない分離を装うことはかえって誠実さを損なう（`CONSTITUTION.md` §1 の裁定優先順 3 位「学習上の有効性と表示上の誠実さを保つ」に照らして、分けない方が誠実である）。
- 受容できなくなる条件: 下記「変更条件」に記載する。

**補正 2（`itemKey` の定義は D-01 の結論に差し替える）**

実装計画 §4.4 の共有スキーマ表が「本ツールが `wordId × rule`」としている箇所は**採用しない**。`itemKey` の定義は下記 D-01 の結論に従う。

**補正 3（H-13 は P0 の阻害要因ではない。P4 着手までに決める）**

H-13（書き出し形式への `product` 判別子追加、`schemaVersion` を 2 へ上げるか）は、P0（境界固定と公開衛生）の実施を妨げるものではないため、P0 では決めない。**P4（保存・移行）の着手までに決める**という期限を記録する。

### D-01: `itemKey` は「1 つの想起行為 = 1 つの問」の単位とする

- `itemKey` の単位は「1 つの想起行為 = 1 つの問」とする。
- 古典学習帳（百人一首アプリ）: `itemKey = poemId × skill`。本文と作者は別の問であるため、別の item として扱う。
- 歴史的仮名遣い確認ツール: `itemKey = wordId`。出題形式は自由入力に一本化済みであり、形式の軸が立たないため、`skill` に相当する第二の軸を持たない。
- 規則（`ruleId`）は習熟度の加点対象に**しない**。`Event` には分析用のキー配列（診断された `ruleId` の列）として持たせ、推薦・規則別モード・「誤答が集中している規則」の集計に使う。
- 表示・上限・5 色の適用単位は、百人一首＝首単位、仮名遣い＝語単位とする（`itemKey` 単位ではない）。

D-01 の理由: 先方設計書は §6 で「習熟度は語ID × 出題形式を単位とする」、§8 で「`itemKey` は `wordId × rule`」と書いており、両者が食い違っていた。`wordId × rule` を蓄積単位にすると、複数規則が絡む語（例: てふ）で 1 回の正答が複数キーへ加点され、`docs/APP_SPEC.md` §8 の上限表と「同一回の再回答は加点半分」の算術が壊れる。規則は 1 つの答案から**診断される属性**であって、別の問ではない。したがって蓄積単位は問の単位に置き、規則は分析次元に降ろす。

### 追補: D-15（2026-08-31）— P5計算コアの再計算契約

発注018の独立監査で、実装計画の「保存済み `delta` を累積」と、規則版更新時の全再計算が両立しないこと、
また推薦について `APP_SPEC` §8.2 の3軸と学習科学監査の4段の対応・期間閾値が未定義であることが判明した。
そこでP5を、確定仕様だけの計算コア018Aと、推薦の仕様確定後に行う残りへ分ける。

- 計算は `Event.delta` を使わず、規則版1と `effectiveMethod` から再導出する。
- 自己評価×は `APP_SPEC` §8.1 と監査の正式係数表どおり `+1 / 上限20` とする。
- 低い方式の上限は正答による加点可能幅だけを制限し、既に高い値を下げない。
- 同一問反復は明示フラグを反映し、偽の場合も `questionId` があるときだけ履歴から補完する。
- ヒント一段下げは正本どおり `free-input → kanji-to-kana → choice → self-tri → view` とする。
- 未対応の規則版イベントは計算へ混ぜず、呼び出し側へ明示的に返す。
- 色境界は 0=灰、1〜29=赤、30〜59=黄、60〜84=青、85〜100=緑。固定段階名は持たない。
- 推薦は期間閾値と3軸/4段の対応が決まるまで実装しない。

いずれも保存層・公開境界・プライバシー・ライセンスを変えない、取り返し可能な技術的表現である。

## 追補: D-07 / D-08（2026-08-30）

D-06 を実装計画 §10 の各フェーズ仕様へ展開する作業のなかで、§4.2 に残っていた `※要確認` 4 件のうち 2 件を裁定した。いずれも D-06 の分類の範囲内であり、新たな構造変更ではない。

### D-07: 静的アセットの置き場

`404.html` は公開単位ごとに複製する。feedback 画像はリポジトリ直下 `assets/feedback/` のまま動かさない。

> #### ⚠ フォントの置き場については、当初の記述が実装されなかった（2026-09-01 に実態へ合わせた）
>
> **当初の記述（2026-08-30）:**
>
> > フォント WOFF2 は `packages/shared/assets/fonts/` を正本とし、各公開単位の `prebuild` が
> > `public/fonts/` へコピーする（コピー先は追跡しない）。
> > 理由: … WOFF2 の実体を git に二重で持たないことを優先し …
>
> **実態（2026-09-01 に実測）:**
>
> | 記述 | 実態 |
> |---|---|
> | `packages/shared/assets/fonts/` が正本 | **存在しない** |
> | 各公開単位の `prebuild` がコピー | **`prebuild` スクリプトは 1 つも無い** |
> | コピー先は追跡しない | **追跡されている。`packages/hyakunin/public/fonts` と `packages/kanazukai/public/fonts` に 371 ファイルずつ** |
> | 実体を git に二重で持たない | **二重で持っている** |
>
> `tools/font-assets-check/index.ts` の `fontsDir()` も
> `packages/<公開単位>/public/fonts` の 2 箇所を正としており、**検査ツールは実態の側に立っている。**
>
> **新たな裁定は不要である。D-12（2026-08-31）が既にこの重複を追認している**——
> 実測 `dist` は hyakunin 8.8MB / kanazukai 8.7MB の計 17.5MB で、
> GitHub Pages の上限 1GB に対し桁が 2 つ違う。共有配信にすると別パス参照になり複雑さだけが増える。
>
> **したがって、記述を実態へ合わせた。** 現在の決定は次のとおりである。
>
> **フォント WOFF2 は `packages/{hyakunin,kanazukai}/public/fonts/` に実体を置き、両方とも追跡する。
> 共有正本もコピー工程も作らない。**
>
> **`packages/shared/assets/` を作らないこと。** 作れば、検査ツールが見ていない 3 つ目の置き場が生まれる。

理由: Vite の `publicDir` は 1 パッケージに 1 つしか指定できないため、`public/` そのものを共有できない。
base path 依存で共有できない `404.html` は複製とした。
フォントの重複は D-12 のとおり容量上の問題にならず、**共有配信にすると別パス参照の複雑さだけが増える。**

### D-08: `load.ts` は共有層に置く

生成 JSON の取得・実行時スキーマ検査・失敗時の ErrorScreen 誘導を `packages/shared/src/data/load.ts` に置き、製品固有のスキーマ記述だけを各公開単位が渡す。

理由: 「生成物が壊れていたら ErrorScreen」は両製品に共通の不変条件である。二重実装は、片方の検査だけが緩んだときに気づけない経路を作る。憲章 §1 の優先順 3（表示上の誠実さ）に直結する。

### 残る `※要確認`（**2026-09-01 に全 4 件を解消または保留へ確定した**）

| # | 論点 | 状態（2026-09-01） |
|---|---|---|
| ① | `public/` を共有するか複製するか | **解消。** D-07（上の是正）＋ **D-12** により**複製で確定**。`packages/{hyakunin,kanazukai}/public/fonts/` に実体を置き両方追跡する |
| ② | 仮名遣い側の `tools/build-data` 構成 | **保留（D-21）。** 仮名遣い実装計画 §9 の提案 1 にあたる。**発注024 で「`tools/build-data/kanazukai/` を作らない」と明記した**（作れば S-C）。再開は百人一首ツールの完成後 |
| ③ | `overflow-check` の仮名遣い対応 | **保留（D-21）。** 仮名遣いツール固有であり、百人一首側の公開を妨げない。`check:overflow` は現在 hyakunin の 1200 件を検査して合格している |
| ④ | `load.ts` を共有層に置くか | **解消。** **D-08** で共有層に置くと確定し、実装済みである（`packages/shared/src/data/load.ts`） |

**4 件とも「決まっていない」状態ではなくなった。**
②③ は**保留であって未決ではない**——決める時期が「百人一首ツールの完成後」と定まっている。
**この表を見て P1〜P7 で停止しないこと。**

---

## 却下した案

| # | 論点 | 却下案 | 却下理由 |
|---|---|---|---|
| 1 | D-06 | 公開リポジトリを 2 つに分ける案 | 共有層（`packages/shared`）を複製するか、npm パッケージとして公開する必要が生じ、初回公開の作業量が増える。`CONSTITUTION.md` と `DESIGN_SYSTEM.md` を 1 本に保てるという利点も失われ、相互リンク（先方設計書が許容する「百人一首アプリからリンクしてよい」）も同一オリジンで完結しなくなる。先方設計書自身が「分離が必要になった時点で別リポジトリへ切り出す」としており、初手で分けない方針と整合しない |
| 2 | D-01 | 実装計画 §4.4 の文言どおり `wordId × rule` を蓄積単位にする案 | 複数規則が絡む語で 1 回の正答が複数の `itemKey` へ加点され、`docs/APP_SPEC.md` §8 の上限表と「同一回の再回答は加点半分」の算術が壊れる。規則は答案から診断される属性であり、別の想起行為（別の問）ではないため、蓄積単位に採用しない |
| 3 | D-01 | 表示も含めて全面 `itemKey` 単位にする案 | `docs/APP_SPEC.md` §8 は「習熟度は首単位の内部指標」と定めており、この案を採るには同仕様の改訂が必要になる。実装計画 §6.3 の推奨（イベントは `itemKey` で記録し、表示・上限・5 色は首単位に適用する）と整合させ、既存仕様を変更せずに監査要求（`itemKey` を独立した履歴単位にする）を満たす経路を優先した |

## この決定が守るもの

- `CONSTITUTION.md` §1（裁定の優先順）3 位「学習上の有効性と表示上の誠実さを保つ」: 利用番号共有をプライバシー境界の判断として明示し、実態のない分離を装わない（補正 1）。
- `CONSTITUTION.md` §6（匿名統計）: 「同じブラウザ保存領域で継続する無作為な利用番号を使う」という条項を、2 プロダクト共有という新しい状況のもとでも文言どおりに満たす。
- `docs/APP_SPEC.md` §8（習熟度は首単位の内部指標）: D-01 により、表示・上限・5 色の単位を首単位（百人一首）・語単位（仮名遣い）に保ち、仕様改訂なしで整合させる。
- `docs/LEARNING_SCIENCE_AUDIT.md` の要求（`itemKey = poemId × skill` を独立した履歴単位にし、歌全体の正解を作者の正解に流用しない）。
- 先方設計書の宣言（`CONSTITUTION.md`、`DESIGN_SYSTEM.md`、習熟度モデル、保存スキーマの共有）を、共有リポジトリ・共有パッケージという構造で実現する。

## 変更条件

次のいずれかが観測されたら、この決定（またはその該当部分）を見直す。

- **利用番号の共有（補正 1）**: 2 プロダクトが別オリジンでの配信に移行した場合（同一保存領域という前提が崩れる）。または、プロダクトごとに利用者層が明確に分かれ、「両方を使っている」という情報が指導上不要な相関を生むと判断された場合。これらが生じたら、番号分離を再検討する。
- **H-13（補正 3）**: P4（保存・移行）の着手前に、書き出し形式への `product` 判別子追加および `schemaVersion` の扱いが未決のままである場合。この場合 P4 に着手できない。
- **公開単位の統合（D-06）**: 2 プロダクトの何れかで利用者数・データ量が増大し、単一リポジトリでの CI・デプロイ時間が実運用上支障をきたす規模になった場合。先方設計書の「分離が必要になった時点で別リポジトリへ切り出す」という条件に該当する。
- **`itemKey` の定義（D-01）**: 仮名遣いツールに出題形式の軸（自由入力以外のモード）が新設され、「形式の軸が立たない」という前提が崩れた場合。この場合 `itemKey` の再定義が必要になる。

## 影響

- 実装計画 §4.1・§4.2 のディレクトリ構成は、本 ADR の構成（`app/` → `packages/{shared,hyakunin,kanazukai}`）へ置き換える。
- 実装計画 §6.1（IndexedDB スキーマ）: `events` ストアに `product` インデックスを足す。
- 実装計画 §6.2（Event の形）: `Event` に `product` を足す。`itemKey` の生成規則は D-01 に従う。
- 実装計画 §6.6（export / import）: 書き出し `schemaVersion` を 2 へ上げるかは H-13 の決着に従う（補正 3 の期限内）。初期化をプロダクト単位で選べるようにする。
- 実装計画 §7.4 相当（統計コレクション設計）: コレクションは env のみで分け、`product` はドキュメント内の次元とする。
- 実装計画 §14.2（PUBLISH_MANIFEST）: 許可リストを共通部（正本 Markdown・LICENSE 一式・`review/`）と公開単位別部（`packages/hyakunin`・`packages/kanazukai`）に分ける。
- 実装計画 P1（最小実行基盤）: 足場を 3 パッケージ分（`shared`・`hyakunin`・`kanazukai`）作る。規模区分が「小〜中」から「中」へ上がる。
- 実装計画 P2（一次資料パイプライン）: 仮名遣いツールの一次データ（`仮名遣い規則_一次データ.md`・`仮名遣い語彙_一次データ.md`、いずれも未作成）と人確認台帳（`review/kanazukai/`）が加わる。両プロダクトが同じ検証枠組み（ADR-0002）に依存する。
- 実装計画 P3（表示基盤 — トークン・フォント・縦書き・あふれ検査）: 共有層に UI 部品を置く。仮名遣いツールの単語モードは横書きであり、縦書き部品は `packages/hyakunin` 側に置く（`CONSTITUTION.md` §7 への追記要否は H-12 として別途未決）。
- 実装計画 P5（習熟度）: 係数を共有層に 1 本で持つ。蓄積単位は D-01 に従う。
- `docs/HANDOFF.md` §4.3 の D-06・H-12・H-13 の状態を「裁定済み」へ更新する必要がある（本 ADR は決定の記録であり、`docs/HANDOFF.md` 自体の更新は本 ADR の作業範囲に含まれない）。

## 追補: H-13（2026-08-31）— 書き出し形式の `product` 判別子

D-06 補正 3 が「H-13 は P4 着手までに決める」と期限を切っていた。2026-08-31、P4 の発注に先立ち
依頼者が **`product` 判別子を足す**ことを裁定した。`docs/HANDOFF.md` §10.5 の切り分けに従い、
**足すか否か**は保存形式の互換性に関わるため人が決め、**表現方法**は下記のとおり Claude が決めて記録する。

### H-13（依頼者裁定）: 足す

判断材料として提示した非対称性は次のとおり。保存機能は P4 で新規に実装するため、
**`schemaVersion: 1` の書き出しファイルは 1 つも存在しない**。いま足せば移行処理は不要である。
公開後に足すと、旧形式を読む分岐を `storage/import.ts` へ恒久的に抱えることになる。
先送りする側にだけ費用が付く。

足さない場合の実害も具体的である。D-06 により IndexedDB は 2 製品で 1 つ、書き出し画面も
2 製品共通であるため、1 つの書き出しファイルに百人一首と仮名遣いの記録が混在する。判別子が
無いと取り込み時にどちらの製品の記録か復元できず、製品別の習熟度再計算（D-01）と統計集計
（D-06 の共有スキーマ 8 項目）が成立しない。

### 表現方法（Claude 決定・§10.5 の 3 条件を満たす）

§10.5 の 3 条件（取り返しがつく／公開物の意味内容・プライバシー境界・ライセンスに触れない／
決定内容と理由を記録する）をすべて満たすため、次を決定し本追補に記録する。

| # | 決定 | 理由 |
|---|---|---|
| 1 | `product` は最上位ではなく `sessions` / `events` / `reports` の**各要素**に持たせる | 1 ファイルに両製品の記録が混在するため、最上位の単一値では混在を表現できない |
| 2 | 値は `"hyakunin"` / `"kanazukai"` とし、`appConfig.products` のキー（`unitName`）と一致させる | 公開パス（`/hyakunin/`・`/kanazukai/`）とも一致する。新しい語彙を作らない |
| 3 | **`schemaVersion` は 1 のまま据え置く** | `schemaVersion: 1` の書き出しファイルは存在しない。2 へ上げると、存在しない旧版のための移行分岐を恒久的に抱えることになり、死んだ経路を検査し続ける費用だけが残る |
| 4 | `product` は**必須**とし、欠落・未知値は `APP_SPEC.md` §9.2 の「不正な形式」として理由つきで拒否する。既定値を推測して補完しない | 省略可にすると既定値を推測する処理が要り、誤った製品へ記録が混入する経路ができる。取り込みは既存記録を壊しうる操作であるため、厳格側へ倒す |
| 5 | `settings` には `product` を付けない | §9.1 の `settings` は端末単位の表示既定である。製品別の設定が必要になった時点で別途裁定する（下記変更条件） |

### 変更条件（H-13）

- 製品別に分けるべき表示設定が現れた場合（`settings` への `product` 付与を再検討する）。
- 3 つ目の公開単位が追加される場合（値の語彙と `appConfig.products` の対応を見直す）。
- 実際に `schemaVersion` を上げる必要が生じた場合（そのときは移行分岐を 1 度だけ導入する）。

---

## 追補: D-17（2026-08-31）— P5 推薦の段・期限・同点（懸案 H-16 の解決）

### 文脈

推薦の仕様正本が 2 つあり、互いに粒度が違ったため `recommend.ts` を発注できなかった。

- `docs/APP_SPEC.md` §8.2 … **3 軸**「未確認日数 → 最近の不正解 → 低い習熟度」。同点は番の昇順。
- `docs/LEARNING_SCIENCE_AUDIT.md`「推薦の優先順」… **4 段**「期限超過 → 直近の要確認 →
  別セッション想起なし → 一定期間未確認」。

さらに **「期限」「直近」「一定期間」の日数がどちらにも書かれていない**。
未定義のまま発注すれば、受け手が黙って数字を決めることになる。

### 決定（D-17・親担当。依頼者が追認済み）

1. **4 段は 3 軸を細分したものである**と読む。監査の段 1 と段 4 はどちらも「未確認日数」の軸であり、
   段 2 は「最近の不正解」、段 3 は「低い習熟度」に対応する。**両者は対立していない。**
2. **未着手（イベント 0 件）を段 4 として足し、全体を 5 段にする。** 監査は再確認の順序しか論じておらず、
   まだ一度も見ていない首の扱いが抜けていた。未着手は「低い習熟度」の極値である。
3. **復習期限は `APP_SPEC` §8 の色境界と同じ帯で決める。** 新しい境界を作らない。
   赤 **1 日**・黄 **3 日**・青 **7 日**・緑 **14 日**。これは監査「間隔スケジュールの試験案」の
   **初期の試験候補 1・3・7・14 をそのまま使ったもの**であり、**最適スケジュールではない。**
4. 段 2 の「直近」は **3 日**。
5. 同点は **(1) 期限超過日数の降順 →(2) 習熟度の昇順 →(3) 番の昇順**。
   `APP_SPEC` の「番の昇順」は最後の決定子として残る。**乱数を使わない**
   （監査「毎回ランダムにして『学習済み』と錯覚させない」）。
6. **該当する首が 1 つも無ければ提案を出さない。** 無理に一件を選ばない。
7. **首の習熟度は、その首に属する項目（`itemKey`）のスコアの最小値**とする。
   イベントを持つが 0% の首は赤帯として扱う（未着手と区別する）。

### なぜ Claude が決めてよいか（§10.5 の 3 条件）

1. **取り返しがつく。** 日数はすべて `packages/shared/src/domain/recommend/rules.v1.ts` の定数であり、
   変更は 1 行である。保存済みの記録に影響しない（推薦は導出値であって保存しない）。
2. **公開物の意味内容・プライバシー境界・ライセンス・権利表示に触れない。**
   推薦は端末内の計算で、外部へ何も送らない。
3. **記録した。** 本追補、`docs/APP_SPEC.md` §8.2、`docs/HANDOFF.md` §4.1、
   `docs/IMPLEMENTATION_PLAN.md` の P5、`docs/CODEX_WORK_ORDER_020.md`。

**§5 の H-16 は「依頼者確認」として起票されていた**ため、日数（1・3・7・14 と直近 3 日）の
妥当性についてのみ依頼者の追認を求めた。**依頼者は同日「1・3・7・14日で進める」と追認した。**
**したがって D-17 は依頼者の追認を得た確定事項である。**

### 変更条件（D-17）

次のいずれかで再検討する。

- **H-06 の試験運用で、提案が「毎日の催促」に感じられると報告されたとき**（赤帯 1 日を延ばす）。
- 別セッション正答率（監査「試験運用では、少なくとも次を記録する」）が帯ごとに偏ったとき。
- 依頼者が日数に異議を述べたとき。

**係数は `RECOMMEND_RULES_VERSION` で版管理する。** 版を上げるときは、
習熟度の `MASTERY_RULES_VERSION` と同じく、旧版のイベントを黙って新版で解釈し直さない。

---

## 追補: D-18（2026-08-31）— 取り込みの原子性

### 文脈

発注012 の検収で、`packages/shared/src/storage/import.ts` の `replaceStore` が
**レコード 1 件ごとに別トランザクションで書いている**ことが判明した。
復旧経路 `restore` も同じ関数を使うため、**復旧の途中で失敗すると既存記録が失われる**。
実装計画の停止条件 **S-7（既存記録の消失）**に直結する。

### 決定（D-18・親担当）

**取り込みの書き込みは、`sessions` / `events` / `reports` / `settings` の 4 ストアにまたがる
ただ 1 つの `readwrite` トランザクションで行う。** 手作りの復旧経路（`readBackup` → `restore`）は
**削除する**。IndexedDB は 1 トランザクション内の失敗を処理系が `abort` して全体を取り消すため、
**原子性は処理系が保証する**。自前の書き戻しは、書き戻し自体が失敗しうるという二重の危険を抱える。

`readBackup` は「既存記録を読んでマージの入力にする」用途では残る。**復旧のためには不要**、という意味である。

### なぜ Claude が決めてよいか（§10.5 の 3 条件）

1. **取り返しがつく。** コードの変更だけで戻せる。保存形式は変わらない（`schemaVersion` は 1 のまま）。
2. **公開物の意味内容・プライバシー境界・ライセンスに触れない。**
   むしろ `APP_SPEC` §9.2「読み込みは既存記録を消さず」を**守れていなかったのを守るようにする**変更である。
3. **記録した。** 本追補、`docs/HANDOFF.md` §4.1、`docs/CODEX_WORK_ORDER_019.md` §3。

### 変更条件（D-18）

実ブラウザで、4 ストアにまたがる `readwrite` トランザクションが
現実的な件数（各 100,000 件・D-14）で破綻することが**実測された**とき。
その場合もストア単位の原子性は保ち、**「途中で止まったら中途半端に残る」形へは戻さない**。

---

## 追補: D-21〜D-27（2026-09-01）— 完成順序と P6・P7 の設計裁定

**この追補は、2026-09-01（第13回）に確定した 7 件の裁定を記録する。**
**§10.5 の条件 3「決めた内容と理由を、該当する ADR と計画の当該節の両方へ記録する」を満たすためのものである。**
（第13回は `docs/HANDOFF.md` §4.1 と各計画節へは書いたが、**ADR への記録を落としていた**。後日補った。）

### D-21（依頼者裁定）: 完成順序は「百人一首 → 仮名遣い」。固有の論点は保留する

依頼者の指示は「共有している部分はそのままでかまわないが、仮名遣いツール固有の問題は保留として進める」である。

**これは 2026-08-31 の D-09（凍結）でも、並走でもない第三の状態である。**

| | 進める | 保留 |
|---|---|---|
| 置き場 | `packages/shared/`、`tools/`、`tests/data/`、検査基盤 | `packages/kanazukai/` だけに効くもの |
| 判定 | **百人一首側も同じ不変条件を必要とする** | 百人一首側には要らない |

**「保留」は「新機能を足さない」であって「壊れてよい」ではない。**
`typecheck` と 2 製品 `build` は両方を対象にしたまま緩めない。
**解除を決めるのは依頼者である。** 読み違いを防ぐ一覧は `docs/KANAZUKAI_HANDOFF.md` §8 にある。

**本裁定は依頼者のものであり、§10.5 の 3 条件による Claude の裁定ではない。**

### D-22: 穴埋め候補の機械生成は「句」単位だけに限る

`APP_SPEC` §7.1 は隠す単位を単語／文節／句の 3 種と定めるが、**機械生成してよいのは句だけである。**

決め手は**データの有無**である。**句境界は `poems.json` の `ku[5]` として既に存在する。**
一方、**古文の語境界・文節境界はデータとして存在しない。** §7.1 自身が
「**確認済みの**文節境界で隠す」と定めており、確認済みのものは存在しない。
文字列から推測すれば形態素解析の代用を当て推量でやることになり、
**発注024 で「助詞・複合語・語義・文法形を文字列から推測しない」と裁定したのと同じ線を越える。**

`blankUnit` の型は 3 値のまま残す（機構は 3 種に対応する）。生成器が作るのは `"ku"` のみ。
単語・文節は `review/blanks.yaml` に人が書いた項目からのみ作る。**推測処理を書いた時点で S-8。**

### D-23: 正解の集合は「変換」ではなく「列挙」で表す

`normalization` の語彙を `"exact"` と `"kana"` の 2 値に固定し、
**歴史的仮名遣いと現代仮名遣いの扱いは `acceptedAnswers[]` / `partialAnswers[]` に
正本から取った文字列を並べて表す。変換規則で導かない。**

決め手は**二重実装の禁止**である。変換で導けば、歴史的仮名遣いツール（`packages/kanazukai/`）と
同じ判定が 2 箇所に生じる。**二重化した検査は片方が無試験のまま残る**ことが、
2026-09-01 の発注023 検収で実証された（`validateData` 側の V-01・V-02 が
`parse.test.ts` の同名検査に隠れて無試験だった）。

**畳み込みの実装は `packages/hyakunin/src/domain/question.ts` ただ 1 箇所に置く。**
`tools/build-data/` はラベルを書くだけで、畳み込みを行わない。

### D-24: 作者 4〜5 択の誤答は乱数を使わず番号距離で決定的に選ぶ

**V-11（同じ入力で 2 回生成した結果がバイト一致）と乱数は両立しない。**

正解の番 N に対し `|cardNo − N|` の昇順・同値なら `cardNo` の昇順に走査し、
`author.canonical` が正解と異なり既選と重複しないものを先頭から 4 件取る。
**4 件に満たない首では形式 1・2 を作らず自由入力だけにする**（`APP_SPEC` §7.2・重点シナリオ R-14）。

### D-25: `domain/` は乱数源と時刻源を引数で受け取る

**新規則ではない。** `packages/shared/src/domain/mastery/` と `recommend/` は
内部時刻・乱数・UTC 日付導出を 0 件に保っており、発注018A・020 の受入条件だった。
**既存の実測済み規約を `domain/{order,session,question}.ts` へ明文化しただけである。**

呼び出し側（P7 の画面）が `Math.random` と当日日付を渡す。
これを守らないと「同一 seed で並びが再現する」ことを機械判定できない。

### D-26（依頼者裁定）: 正解基準は「歴史的仮名遣いまたは漢字」。現代仮名遣いは部分正解

| 入力 | 判定 | 習熟度 |
|---|---|---|
| 漢字表記 | 正解 | 方式どおり |
| 歴史的仮名遣い | 正解 | 方式どおり |
| **現代仮名遣い（ひらがな）** | **部分正解** | **プラス。ただし方式を一段下げる** |
| それ以外 | 不正解 | 減分 |

**部分正解のときは「歴史的仮名遣いではどう書くか」「漢字ではどう書くか」を必ず返す。**
**「試験前の確認」（`exam` 入口）も同じ扱いとする。**

**新しい係数を作らなかった。** 一段下げは `APP_SPEC` §8.1 のヒントと同じ表（`HINT_METHOD_DOWNGRADE`）を使う。
**D-17 で確立した「新しい境界を作らない」と同じ方針である。**
紙手書き自己申告は自由入力と同じ増分なので一段下げ先は漢字候補（+7 / 上限 80）とする。
**この 1 行の表追加は P7 で試験つきで行う**——検収済みの `mastery/rules.v1.ts` に無試験の分岐を足さないためである。

**設定で正解基準を段階から選ぶ機能は初回公開では作らない**（依頼者「現時点では不要」）。**先回りしたら S-C。**

実装上は `Question` に `answerHistorical` / `answerModern` / `acceptedAnswers[]` / `partialAnswers[]` を、
`Judgement` に `'partial'` を足す。**表示用（`answer*`）と判定用（`*Answers[]`）は役割が違うので両方を持つが、
生成器の同じ 1 箇所から同時に作る**ので食い違わない。

### D-27: 画面テスト基盤の依存は `jsdom` 1 件だけ

実測（2026-09-01）: ルートの `npm test` は `.tsx` を拾わず、`vitest.config.ts` に `test` 設定も
DOM 環境も `test` スクリプトも無い。**「画面が壊れていないこと」を機械で確かめる手段が 1 つも無い。**
これは **D-14 の 4** が P7 へ先送りした件である。

描画は `preact` の `render`、状態更新の待ち合わせは **`preact/test-utils` の `act`**（preact に同梱）、
要素取得は素の DOM API で足りる。**したがって依存は `jsdom` 1 件で済む。**
`@testing-library/*` は入れない——**便利さのために依存を増やさない**（依存 1 件ごとに S-13 を背負う）。
`happy-dom` を選ばないのは、実装差が出たとき `check:overflow`（実 Chromium）と突き合わせる手間が増えるためである。

版は `--save-exact` で固定し、**ライセンスは `node_modules/jsdom/LICENSE*` を実際に読んで
`docs/LICENSE_AUDIT.md` へ記録する（記憶で「MIT」と書かない）。**

### なぜ Claude が決めてよいか（§10.5 の 3 条件）— D-22〜D-25・D-27

**D-21 と D-26 は依頼者裁定であり、この判定の対象外である。**

1. **取り返しがつく。** いずれもコード・生成物・依存の変更だけで戻せる。
   保存形式（`schemaVersion`）は変えない。`questions.*.json` は再生成可能な派生物である。
2. **公開物の意味内容・プライバシー境界・ライセンスに触れない。**
   D-22 はむしろ **F-06「未確認データを公開出題に混ぜない」を守る側**へ倒す裁定である。
   D-27 の `jsdom` は **devDependency であり公開物に含まれない**。
3. **記録した。** 本追補、`docs/HANDOFF.md` §4.1、`docs/APP_SPEC.md` §7.1.1・§10.1、
   `docs/IMPLEMENTATION_PLAN.md` §5.2・P6、発注書 026・027・028・029 の §3。

### 変更条件（D-22〜D-27）

| 裁定 | 見直す条件 |
|---|---|
| D-22 | 確認済みの語境界・文節境界が `review/blanks.yaml` にデータとして存在するようになったとき。**そのときも生成器は台帳を読むだけで、推測はしない** |
| D-23 | 歴史的仮名遣いツールが完成し、その変換が**単一の正本として両プロダクトから使える**形になったとき。**それまでは列挙で表す** |
| D-24 | 誤答の質が実運用で問題になったとき（H-06）。**そのときも乱数は使わない。** V-11 が先である |
| D-25 | 無し。**この規約は緩めない** |
| D-26 | 依頼者が「設定で正解基準を段階から選ぶ」機能を必要と判断したとき（後続段階）。**一段下げの係数は H-06 の試験運用で校正する** |
| D-27 | `jsdom` で表現できない検査（実ブラウザ固有の描画・フォント計測）が必要になったとき。**そのときは `check:overflow` の Playwright 側へ寄せる。DOM 側の依存を増やさない** |
