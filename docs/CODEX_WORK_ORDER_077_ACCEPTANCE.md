# 発注077 検収記録

実装日: 2026-09-08
基準HEAD: `0c21dbf0c5b88dc93d25ddc51e376e0c151b17b4`（発注書記載の基準）
実装モデル: Codex / GPT-5（この実行環境ではモデル版・推論強度の追加識別子は開示されていない）

## 完成差分

- `packages/hyakunin/src/ui/screens/Session.tsx`: 非本番の作者問題で「わからない！」の閲覧保存が成功したときだけ、既存の `PartialSupplement` / `AnswerLines` による作者の正解表示を「答えを確認しました。」に続けて出すようにした。
- `tests/screen/session.test.tsx`: 作者の閲覧保存成功・保存失敗・本番の先行非開示を直接検査する077回帰試験を追加した。
- `docs/APP_SPEC.md`: 保存成功後だけ作者正解を示し、本番では採点一覧まで開示しないこと、および `viewed` を維持することを明文化した。

イベント生成・判定・再確認選定・候補・問題データ・本番の保存時機は変更していない。既存の未関連差分と未追跡ファイルは変更していない。

## 受入条件

1. 合格。`author` の作者fixtureで「わからない！」を押し、保存成功後に「答えを確認しました。」と `正解：持統天皇　持統天皇` を確認した。既存の作者表示コンポーネントを再利用しているため、読みの異なる実データでも同じ表記規則を用いる。
2. 合格。本文の既存閲覧試験を含む画面試験が通過し、本文は問題文の空欄への開示、閲覧イベントは1件という既存動作を維持した。
3. 合格。本番作者fixtureで「わからない！」後にフィードバック・正解補足・「答えを確認しました。」が存在せず、次問の選択肢だけが表示されることを直接検査した。採点一覧での正解表示は既存経路のままである。
4. 合格。作者の閲覧保存を失敗させたfixtureで、失敗案内を表示し、正解・成功文を出さず、同じ候補と「わからない！」を残すことを直接検査した。
5. 合格。成功fixtureのイベントは1件で `kind: "view"`、`outcome: "viewed"`、`itemKey: "p010:author"`、元の `questionId` を維持した。既存の結果集計試験「再確認には『わからない！』で答えを見た問を含める」も全体試験で通過した。
6. 合格。作者成功時にinputを作らないことを検査した。イベントは1件で、操作・通知・遷移の重複は追加していない。
7. 合格。正解は通常のテキスト要素として `aria-live="polite"` の既存フィードバック領域にある。新しいビルド後の `check:overflow` と `check:eol` は成功した。

## 実行結果

| 実行 | 結果 |
|---|---|
| `npm run test:screen -- tests/screen/session.test.tsx tests/screen/feedback-timing.test.tsx` | 71 passed |
| `npm test` | node 530 passed、画面試験成功 |
| `npm run typecheck` | 成功 |
| `npm run build` | 両アプリ成功 |
| `npm run check:overflow` | 成功 |
| `npm run check:eol` | 成功 |

## 未確認事項

- U2以降の表示案、075・076の再実装は本発注の範囲外として未実施。

## 独立最終検収（2026-09-08）

実装を行っていない別のCodexタスクが、基準HEAD `0c21dbf0c5b88dc93d25ddc51e376e0c151b17b4` と完成差分を読み直して検収した。

### 077の実測

- 対象画面試験71件、全Node試験530件、全画面試験292件、型検査、両アプリのビルド、改行検査がすべて終了コード0だった。
- 実ブラウザで作者入口から「わからない！」を操作し、幅320 / 375 / 414 / 768 / 1024px、文字100% / 200%の10条件すべてで「答えを確認しました。」、正式な作者名、現代読み、異なる歴史的仮名遣いを確認した。`aria-live="polite"` を持ち、横あふれ・対象要素の切れは0件だった。
- 保存失敗時と本番解答中の先行非開示、`view` / `viewed`、作者の `itemKey` と元の `questionId`、再確認対象の維持を試験と差分の両方で確認した。

### overflow再検収

最初の独立実行では `tools/overflow-check/index.ts` の紙回答本番走査で「次へ」を待ってタイムアウトしたため、成功扱いにしなかった。限定調査では、カード1に確認済みイベントがある同じ保存条件で紙回答本番を20回実行し、毎回10問・採点一覧10行へ到達した。さらに検査器と同じ待機なしの10回クリックを50回連続で実行し、失敗0件だった。製品側の遷移不具合と恒常的な検査器不具合は再現せず、一時的な実行失敗と判定した。待機時間の延長、対象除外、失敗の握りつぶし、製品・検査器のコード変更は行っていない。

その後、`npm run build` を終了コード0で実行してから、既定条件の `npm run check:overflow` を再実行した。走査は終了コード0で完走し、歌の表示1800 / 1800件、初回設定12件、拡大12件、出題12件、本番出題24件、作者12件、復元24件、057表示24件、本番採点12件、本番範囲選択24件のすべてが違反0件だった。

以上により、発注077の受入条件1〜7と必須ゲートを満たすため、独立最終検収を合格とする。
