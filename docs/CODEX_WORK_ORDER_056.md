# 発注056（054 検収で見つかった試験の穴と走査経路を補う）

- **宛先**: **Terra（Codex）**
- **起草**: 2026-09-05・Astra（親担当の依頼による）
- **状態**: **草案・未発行。親担当が検算してから発行する。本文は着手許可ではない。**
- **入力の正本**: `docs/CODEX_WORK_ORDER_054.md` §10.2〜§10.4
- **規模**: 小〜中（試験の追加・整理と検査ツールの経路追加）

---

## 0. 着手条件（発行時に親担当が確認する）

> **親担当が本書を発行したあとに着手すること。**
> **製品の実装は正しい。変更するのは試験の網と overflow 検査の走査経路だけである。**
> **発注054 の検収結果を本書で確定しない。発注055 と親担当の当て直しは別工程である。**

| 検査 | 基準線 |
|---|---|
| `git diff --stat 57dccdb HEAD -- packages tests` | **0 行**。`57dccdb` は実装が最後に動いた commit。**HEAD のハッシュ一致は要求しない。以後の docs だけの commit は停止理由にしない** |
| `git diff --stat 57dccdb HEAD -- tools/overflow-check` | **0 行**。今回変更する検査ツールの基準も確認する |
| `git diff` と `git diff --cached` | **ともに空**。未追跡ファイルも着手時に一覧を記録し、見覚えのないものを削除しない |
| `npm run test:node` | **450 passed / 0 failed** |
| `npm run test:screen` | **15 files / 128 passed / 0 failed** |

試験数は054 §10.1の親担当の実測値である。Terra は着手時に採り直す。草案作成時の新たな実測値ではない。
発行前に親担当が本書を保存・commit する。文書の未保存差分がある場合も、自分で消さず報告する。

---

## 1. なぜこの発注が要るのか

054 §10.3には、実体が正しいのに試験が無い性質が4件ある。
§10.4では、拡大時の overflow 検査がホームしか開かず、本番の採点一覧を一度も描画していない。
§10.2では、`T-6` を単独で赤にする破壊が無く、行数の検査が他の釘と独立に効くか未証明である。

本書が扱うのはこの3点だけである。機能の追加や製品の修正は行わない。

## 2. 完成形

1. 採点前に閉じた回の注意書き、`FlowPhase` の5値、紙の採点一覧の歌番号と正答、本番採点の△基準が、自動試験で留まっている。
2. `check:overflow` が実際の操作で本番を解き終え、採点一覧を表示した状態で幅320px・文字200%を含む測定を行う。到達しなかった場合は合格しない。
3. `T-6` の整理を破壊の実測に基づいて判断し、他の釘を弱めずに結果を残す。

## 3. 裁定済み事項（決め直さない）

### 裁定1: 製品の実装を変更しない

`packages/**` は恒久変更禁止。CSS、文言、判定、保存、出題内容も変更しない。
4件とも054 §10.3で実体は正しいと確認済みである。追加試験や実走査が製品の不具合を示した場合は、試験を弱めず親担当へ報告する。

### 裁定2: 注意書きは描画結果を見る

本番の画面入力・紙の両方を実際に最後まで進め、採点確定前の採点一覧に
「採点が確定するまで習熟度は保存されません。途中で閉じた場合は記録されません。」
が表示されることを検査する。ソースの文字列検索だけでは受け入れない。
復元機能を作ること、解答中へ文言を移すこと、新しい文言を足すことは本書の対象外。

### 裁定3: FlowPhase は宣言の全構成員を比較する

`packages/hyakunin/src/domain/flow.ts` の `FlowPhase` 型宣言を静的に読み、
構成員を `prompt` / `answered` / `revealed` / `save-failed` / `complete` の集合と完全一致で比較する。
既存の TypeScript API で型宣言を解析し、宣言がちょうど1件、全構成員が文字列リテラル、件数が5件であることも assert する。
5語を含むだけの検索や、試験側に書いた配列だけの検査では受け入れない。6値目の追加を検出できること。
試験は `tests/screen/**` に置く。依存パッケージは追加しない。

### 裁定4: 紙の正答は行に結び付けて見る

異なる歌番号・異なる正答を持つ2問以上を紙で最後まで進め、採点一覧の各行に対応する歌番号と正答が表示されることを検査する。
走査前に対象行数が問題数と一致し、問題数が0でないことを assert する。
画面全体に正答の文字があるだけの検査では受け入れない。`T-7` は画面入力の検査として残す。

### 裁定5: 本番の△は実際の入力から検査する

歴史的仮名遣いと現代仮名遣いが異なる、確認済み相当の fixture を使う。
本番の画面入力で、正答相当・現代仮名遣いのみの部分正解・誤答の3問を解き終え、
採点一覧の対応する行の表示と accessible label が順に ○・△・× であることを比較する。
判定関数や `Session` を mock して△を注入しない。fixture の正答と部分正解は異なる値とし、既存の `acceptedAnswers` / `partialAnswers` の規約に合わせる。
練習の既存試験を本番の検査の代用にしない。紙の△に新しい基準・説明を作ることは対象外。

### 裁定6: overflow は画面入力の本番採点一覧を1経路追加する

既存の閲覧1200件とホームの拡大8件を残す。本番の**画面入力**の採点一覧を新たな走査対象とし、
幅 `320 / 375 / 414 / 768` × 文字 `100% / 200%` の**8件**を追加する。
紙の経路をさらに追加することは本書では要求しない。

ホームから「学習方法を選ぶ」→「本番のように解く」→開始前画面→画面入力で開始し、
通常の入力と送信操作で全問を解く。範囲は1〜10番・番号順とし、予定問題数10件を確認する。
一覧へ直接 state を注入したり、一覧のHTMLをツール側で作ったりしない。
解答には空欄ではない固定文字列を使ってよい。正解であることは走査の条件ではない。

各幅・文字倍率について「採点する」の見出しと `.grade-list > li` の10行を確認し、
拡大を適用した状態の `main *` を既存と同じ1px許容の横方向 overflow / clipping 判定で測る。
対象要素数が0ではないことを確認し、測定後に初めて本番の走査件数を加算する。
8件のそれぞれについて画面名・幅・文字倍率・予定問題数・行数・測定要素数・違反数を出力する。
ホーム8件と本番8件を別に集計し、件数の不足・過剰、未到達、例外、timeout のどれでも終了コードを非0にする。
画面遷移の待機は有限にし、失敗を握り潰して次へ進まない。

### 裁定7: T-6 は既存の釘を弱めずに検討する

まず既存の `T-6` を残して、§6のF-M5・F-M6を実測する。
`T-7`・`T-8`・`T-9`・`R-8` や追加する4件の assert、fixture、実行対象を、単独赤にする目的で変更しない。
試験名・sessionId・fixture 固有の値を条件に製品を壊す方法も不可。

結果に応じた扱いは次のとおりに固定する。

- **`T-6` 単独赤が得られる**: 行数を留める釘として残し、その破壊行と全失敗名を記録する。
- **単独赤が得られない**: `T-6` を残す。重複して赤くなった試験名と各 assert の対象を表にし、「独立性は未証明・削除せず維持」と記録する。この結論も本書の整理検討の完了として受け入れる。

単独赤が無いことだけを欠陥や停止理由にしない。削除・統合・他の釘の弱化は本書では行わない。

### 裁定8: 測定前の build と復元を省かない

通常測定も製品ソースを一時的に壊した overflow 測定も、必ず `npm run build` 成功後に走らせる。
破壊ごとに `cp` で控え、適用後の `grep -c` が0でないことを確かめてから測定し、`cp` で戻す。
復元後はその行の直前との差が空であることを `git diff --no-index <控え> <対象>` で確認する。
控えは作業ツリーの外に置く。複数の破壊を重ねない。
1回の検査実行は5分を上限とし、超えたらその実行と子プロセスを中断して復元し報告する。
破壊中の複数赤は停止理由ではない。復元後の赤は停止理由である。

## 4. 変更境界

| 恒久変更してよいファイル | 範囲 |
|---|---|
| `tests/screen/feedback-timing.test.tsx` | F-1〜F-4を追加。既存T-1〜T-11のassertは維持 |
| `tools/overflow-check/index.ts` | 本番採点一覧への経路・到達検査・件数検査・測定結果の出力 |
| `docs/CODEX_WORK_ORDER_056.md` | 発行後、Terra が§6の実測欄と§9の完了報告を記入することだけ |

**上記以外は恒久変更禁止。** 特に `packages/**`、他の既存試験、`package.json`、lockfile、
`tools/font-weight-check/**`、054・055・追補・`RESOURCE_LOG`・`HANDOFF` は変更しない。
§6が指定する対象のみ、破壊試験の間の一時変更を認める。
終了時は着手時HEADに対する `git diff --name-only <着手時HEAD>` と未追跡ファイル一覧で許可外変更0件を確認する。
さらに `git diff --numstat <着手時HEAD> -- packages` が0行であることを報告する。

## 5. 釘の一覧

名前は `F-1:`〜`F-4:` を先頭に付ける。各1本、合計4本を追加する。

| # | 留めるもの | 判定 |
|---|---|---|
| F-1 | 採点前に閉じた回の注意書き | 裁定2の両モードで完全な文言の描画を確認 |
| F-2 | FlowPhaseを増やさない | 裁定3の宣言件数・型形状・全構成員の完全一致 |
| F-3 | 紙の採点一覧の歌番号と正答 | 裁定4の非空・行数一致・各行の対応を比較 |
| F-4 | 本番採点の△基準 | 裁定5の実入力と行ごとの○・△・×を比較 |

overflow の到達と測定は裁定6の実走査で判定する。ツールのソースにセレクタが存在するだけでは代用にならない。

## 6. 破壊試験（期待値・実測欄は空欄で渡す）

F-M1〜F-M6は毎行 `npm run test:screen` と `npm run test:node` を実行する。
F-M7・F-M8は `npm run build` 成功後に `npm run check:overflow` を実行する。
各行のgrepの検索式と件数、試験の完全名、passed / failed、終了コードを実測欄に記録する。
赤が複数なら全部書く。空欄を予想で埋めない。

| # | 壊し方 | 期待値 | 実測（grep・赤い試験名または検査キー・総数・終了コード） |
|---|---|---|---|
| F-M1 | `Session.tsx` の採点一覧から「途中で閉じた場合は記録されません。」だけを削除する | | |
| F-M2 | `flow.ts` の `FlowPhase` に6値目の文字列リテラル `grading` を追加する | | |
| F-M3 | `Session.tsx` の紙側の分岐だけから `正答: {item.answer}` のspanを削除する。画面入力側は残す | | |
| F-M4 | 画面入力の採点一覧で `partial` の表示文字とaria-labelを、両方とも△から○へ変える。判定関数は触らない | | |
| F-M5 | 054 M-7を再測定する。採点一覧の `rows.map` を `rows.slice(0, -1).map` にして最終行を落とす | | |
| F-M6 | 採点一覧の `rows.map` を `[...rows, ...rows.slice(-1)].map` にして最後の行を1行重複させる。入力方式などで条件分岐しない | | |
| F-M7 | `Session.tsx` の採点一覧の `ol` に一時的に `style={{ minWidth: "2000px" }}` を付けて横にはみ出させる | | |
| F-M8 | overflowツールの新設した本番経路だけを、ホームの読み込み後・本番開始前に打ち切る。予定8件の検査・到達検査・件数加算条件は残す | | |

F-M1・F-M3は削除対象の置換前件数と、置換後の該当箇所に一時マーカーを置いた件数を `grep -c` で記録する。
他の行も置換後の式または一時マーカーで適用を確かめる。0件なら測定不能として止める。

## 7. 受入条件

### 7.1 通常ゲート

復元後に次をすべて実行し、終了コード0を確認する。

`npm run typecheck` / `npm run lint` / `npm test` / `npm run data:check` /
`npm run build` / `npm run scan:publish` / `npm run check:eol` /
`npm run check:overflow` / `npm run check:font-weight`

| 検査 | 受入値 |
|---|---|
| test:node | 450 passed / 0 failed / 0 skipped |
| test:screen | 15 files / 132 passed / 0 failed / 0 skipped（128＋4。既存試験を削除しない） |
| scan:publish / check:eol | 違反0 |
| overflow・閲覧 | 1200件ちょうど・違反0 |
| overflow・ホーム拡大 | 8件ちょうど・違反0 |
| overflow・本番採点一覧 | 8件ちょうど・全件10行・測定要素数が全件正・違反0。320px / 200%を含む |
| font-weight | 終了コード0 |

### 7.2 試験の感度・検査の成立

- F-M1〜F-M4でそれぞれ対応するF-1〜F-4が失敗すること。その他の赤も全件記録する。
- F-M5でT-6が失敗すること。F-M5・F-M6の両方の全失敗名とT-6単独赤の有無を記録し、裁定7の条件分岐に一致した結論を残すこと。
- F-M7で本番採点一覧のoverflowまたはclipping違反が1件以上となり、終了コードが非0であること。
- F-M8で未到達または本番走査件数不足を報告して終了コードが非0となること。合格表示を出さないこと。
- §6の全8行が適用確認済み・実測記入済みであること。timeoutや未実行を合格扱いしないこと。

### 7.3 変更境界・復元

§4の許可外差分0件、`packages` のnumstat 0行、控えの残存0件。
既存のT-1〜T-11・R群・N群・`no-pressure.test.tsx` が全て緑であること。
破壊からの復元を済ませたソースで再buildし、そのbuildへのoverflow最終測定を行うこと。

## 8. 停止条件

| # | 条件 |
|---|---|
| S-1 | §0の差分または試験数が基準線と異なる。HEADハッシュが違うだけでは止めない |
| S-2 | 製品・CSS・判定・保存など、§4の許可外の恒久変更が必要と判断した |
| S-3 | 追加した試験や通常走査が、復元した製品で失敗する。実測を報告し、製品も閾値も直さない |
| S-4 | grepが0件、5分を超える実行、またはcp復元後に控えとの差分が残る。復元を試みた事実を記録し、自分で辻褄を合わせない |
| S-5 | 復元後に既存試験が赤い、またはscan:publishに違反が出る。破壊中の赤には適用しない |
| S-6 | 本番が予定10問で開始できない、採点一覧に到達できない、または測定件数の検査を満たせない |
| S-7 | §7.2の感度条件を満たさない。T-6の単独赤が無いこと自体は除く（裁定7） |
| S-8 | 本書に無い仕様判断や変更境界の拡張が必要。親担当に報告する |

停止時にも破壊の復元は行う。親担当の裁定なしに対象を拡張しない。

## 9. 完了報告（発行後にTerraが記入）

1. §6の全8行の実測と、各行のcp復元確認。
2. F-1〜F-4の追加本数、通常ゲートの件数・終了コード。
3. 本番採点一覧8件の到達・行数・測定要素数・違反数の出力。
4. T-6の単独赤の有無、併発した釘とassertの対象、裁定7に従った結論。
5. 着手時HEADに対する変更ファイル一覧と、packagesのnumstat 0行の出力。
6. 破壊復元後のbuildと最終ゲートの実測。
7. 判断が必要だった点。無ければ「なし」。
8. 全体・破壊行ごとの時間、適用失敗・復元失敗・timeoutのやり直し回数、使用モデル、Codex CLI表示のトークン数（自己申告値。表示が無ければ取得不可と記し、推定しない）。

---

**実行していない検査を成功と書かない。検査ツールは完走しなかったときに合格を出してはならない。**
**本書は草案であり、親担当の検算と発行をもって着手可能となる。**
