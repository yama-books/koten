# Codex向け発注文書 022: 発注020 で証明力が無いと実測された試験の穴 4 件

発注日: 2026-08-31（第9回セッション）
階層: **Terra**（仕様は確定済み。受入条件はすべて機械判定できる）
優先度: 中（**P5 の実装そのものは検収に通っている。** これは試験の証明力を回復させる発注である）
対象: `tests/unit/recommend/`（**既存ファイルへの追加**）

> **並行発注に関する注意。同時に次が走っている。衝突するファイルに触らないこと。**
>
> | 発注 | 階層 | 触っている領域 |
> |---|---|---|
> | 021 | Terra | `tests/unit/storage/atomicity.test.ts` |
>
> 本発注は `tests/unit/recommend/` の**外に出ない**。`tests/unit/storage/` に触らない。

---

## 0. この発注の位置づけ（**先に読むこと。誤解すると実装を壊す**）

**発注020 の実装は 2026-08-31・第9回に親担当が検収し、合格させた。**
`packages/shared/src/domain/recommend/` の 2 ファイルは `APP_SPEC` §8.2 と全数照合して一致しており、
**1 バイトも変更してはならない。**

問題は**試験の側**にある。親担当が実装の論理を 1 箇所ずつ反転させたところ、
**次の 4 つの反転はどの試験も赤くしなかった**。すなわち、これらの仕様は
「試験に守られていない」。実装が正しいのは**偶然ではないが、証明されていない**。

| 反転 | 実装への変更 | 結果 |
|---|---|---|
| B-7 | 段 5 の行をまるごと削除する | **93 件すべて緑のまま** |
| B-8 | `dayNumber` の平年月別通日表を 1 日ずつずらす | **93 件すべて緑のまま** |
| B-9 | `reviewColor` の「0% ＋ イベントあり → 赤帯」を外す | **93 件すべて緑のまま** |
| B-10 | 段 3 の理由文をでたらめな文字列に変える | **93 件すべて緑のまま** |

**本発注は、この 4 つの反転が名指しで赤くなるようにすることだけを目的とする。**

---

## 1. 先に読むもの

| 文書・ファイル | 読む箇所 | 何のために |
|---|---|---|
| `docs/APP_SPEC.md` | **§8.2 全体** | **仕様の正本。** 段 5・理由文・0% の扱い |
| `docs/CODEX_WORK_ORDER_020.md` | §4・§5 | 既存試験 A-1〜A-11 が何を固定しているか |
| `tests/unit/recommend/recommend.test.ts` | 全 95 行 | **書き方を合わせる。** 既存試験を消さない・緩めない |
| `tests/unit/recommend/fixtures.ts` | 全 10 行 | `event()` の既定値 |
| `packages/shared/src/domain/recommend/recommend.ts` | 全 122 行 | **読むだけ。変更禁止** |

---

## 2. 変更境界

### 変更してよいファイル

| ファイル | 変更の種類 |
|---|---|
| `tests/unit/recommend/recommend.test.ts` | **試験の追加のみ。** 既存 A-1〜A-10 を削除・改変しない |
| `tests/unit/recommend/fixtures.ts` | 必要なら補助関数の追加のみ |

### 絶対に変更しないファイル・領域

- **`packages/` の下すべて**（**実装は検収済み。1 バイトも変えない**）
- `tests/unit/storage/`（**発注021 が同時に触っている**）
- `tests/unit/mastery/`、`tools/**`、`.github/**`、`docs/**`、すべての `package.json`
- **新しい npm 依存を入れない**

---

## 3. 裁定済み事項（再検討しないこと）

| # | 裁定 | 出典 |
|---|---|---|
| 1 | **実装は正しい。試験だけを足す。** 試験を書いた結果が赤くなったら、それは**試験の書き間違い**である。実装を直して合わせてはならない（S-1） | 第9回の検収 |
| 2 | 段 5 は「復習期限を過ぎており、かつ**別の日の想起成功が既にある**」首である。理由文は段 1 と同じ「前回から間隔が空いたため」だが、**`tier` は 5 である** | `APP_SPEC` §8.2 |
| 3 | **イベントがあって 0% の首は赤帯**として扱い、期限は 1 日とする。未着手（イベント 0 件）だけが段 4 であり、期限を持たない | `APP_SPEC` §8.2「0% の扱い」 |
| 4 | 暦日差は `'YYYY-MM-DD'` から通日を作って求める。**月・年・うるう年をまたいでも正しい**必要がある | 発注020 §4.3 |
| 5 | 理由文は `APP_SPEC` §8.2 の文言と**完全一致**である。言い換えを許さない | `APP_SPEC` §8.2「理由の表示」 |
| 6 | **「直近 3 日以内」は暦日差 3 以下**（今日・1 日前・2 日前・3 日前）である。既存の A-5 が正しい。**変更しない** | **裁定 D-19**（2026-08-31・親担当。`APP_SPEC` §8.2 に明記済み） |

---

## 4. 追加する試験（**受入条件**）

**すべて `recommend.test.ts` に追加する。既存の命名（`A-n:` 形式）に倣い、`C-n:` を使う。**

| # | 固定する仕様 | 最低限の中身 |
|---|---|---|
| **C-1** | **段 5 が段 5 として返る** | 期限を過ぎており、かつ**別の日の想起成功が 2 日分ある**首だけの範囲で、`tier === 5`・`reason === '前回から間隔が空いたため'`・`poemId` を **`deepEqual` で全項目**固定する。段 4 の未着手首を混ぜないこと（段 4 が勝ってしまう） |
| **C-2** | **暦日差が月・年・うるう年をまたいで正しい** | 次の 3 件を別々の試験にする。いずれも**黄帯（習熟度 30〜59%・期限 3 日）・イベント 1 件のみ・別日想起なし**の首 1 件だけの範囲とし、**期限の両側**を見る。**差 3 なら `tier === 1`、差 2 なら `tier === 3`** である（下の値は親担当が実装に対して実測済み）。<br>(a) **月またぎ**: `today = 2026-03-01`、学習日 `2026-02-26`（差 3）と `2026-02-27`（差 2）<br>(b) **年またぎ**: `today = 2026-01-01`、学習日 `2025-12-29`（差 3）と `2025-12-30`（差 2）<br>(c) **うるう日**: `today = 2028-03-01`、学習日 `2028-02-27`（差 3）と `2028-02-28`（差 2）。**2028 年はうるう年で 2 月 29 日が存在する** |
| **C-3** | **0% ＋ イベントありは赤帯（期限 1 日）** | 習熟度 0%・イベントあり・最終学習日が 1 日前・別日想起なしの首が **`tier === 1`** で返ることを固定する。**あわせて、同じ首の最終学習日を当日にすると `tier === 3`**（期限内・別日想起なし）になることも固定し、「赤帯として期限が効いている」ことを両側から示す |
| **C-4** | **段 2・段 3・段 5 の理由文が正本と一致する** | 段 2・段 3・段 5 それぞれについて、`reason` を `APP_SPEC` §8.2 の文言と**文字列一致**で表明する。段 5 は C-1 と兼ねてよい |

### 4.1 満たすべき全体条件

| # | 条件 | 判定方法 |
|---|---|---|
| D-1 | `npm test` / `npm run typecheck` / `npm run lint` がいずれも終了コード 0 | 各コマンド |
| D-2 | 試験件数が**着手時より増えている**（減っていない）。**着手時の件数を最初に自分で測り、報告に両方書くこと。** 発注021 が同時に `tests/unit/storage/` へ試験を足しているため、**固定の件数を前提にしない** | `ℹ tests` の行を着手時と完了時の 2 回 |
| D-3 | `packages/` の下が**1 バイトも変わっていない** | `sha256sum packages/shared/src/domain/recommend/*.ts` が下記と一致すること |

```
4075e0610d17f11c286a111a9d453ac4091179a483a890ca2ca39d86531d89ba  packages/shared/src/domain/recommend/recommend.ts
ff21b46b04a00b1b27536aa31705e789163fab93f01d11a0ede7b9e0ed76c82f  packages/shared/src/domain/recommend/rules.v1.ts
```

---

## 5. 破壊試験（**受入の中心**）

**「試験を足しました。全部緑です」は受け付けない。**
足した試験が証明力を持つことを、**実装を 1 箇所ずつ反転させて**示すこと。

| # | 反転させる論理（`recommend.ts` / `rules.v1.ts`） | 赤くなるべき試験 |
|---|---|---|
| **B-7** | `recommendNext` の段の表から `[5, (status) => status.overdueDays > 0],` の行を削除する | **C-1**（および C-4 の段 5） |
| **B-8** | `dayNumber` の平年の月別通日表を `[0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334]` から `[0, 31, 58, 89, 119, 150, 180, 211, 242, 272, 303, 333]` へ変える | **C-2 (a) と (b)**（実測: (a) 差 3 が段 3 に落ち、(b) 差 2 が段 1 に上がる） |
| **B-8b** | `isLeapYear` を `return false;` に変える | **C-2 (c)**（実測: 差 3 が段 3 に落ちる） |
| **B-8c** | `dayNumber` の `completedYears * 365` を `completedYears * 366` へ変える | **C-2 (b)**（実測: 差 2 が段 1 に上がる） |
| **B-9** | `reviewColor` の `return color === 'gray' && hasEvents ? 'red' : color;` を `return color;` へ変える | **C-3** |
| **B-10** | 段 3 の理由文 `'別の日にも思い出せるか確かめましょう'` をでたらめな文字列へ変える | **C-4**（段 3） |

**さらに、既存の B-1〜B-6（発注020 §5.1）が引き続き赤くなることを確かめること。**
**試験を足した結果、既存の反転が検出できなくなっていないこと**を示すためである。

**反転させた実装は必ず元へ戻し、戻した後の `sha256sum` が §4.1 の D-3 の値と一致することを報告に含める。**

---

## 6. 停止して報告する条件

| # | 条件 |
|---|---|
| **S-1** | **`packages/` を変更しないと試験が緑にならないと判断したとき。** 実装は検収済みである。**実装を直さず、そのまま報告する** |
| **S-2** | `APP_SPEC` §8.2 と本書が食い違うと気づいたとき。**`APP_SPEC` が正本である。従わずに報告する** |
| **S-3** | 既存 93 件のいずれかが赤くなり、その原因が本発注の追加にあると判断したとき |
| **S-4** | 新しい npm 依存が必要だと判断したとき |
| **S-5** | 画面（`ui/`）に接続したくなったとき。**本発注は試験だけである** |

---

## 7. 完了報告に含めること

1. 変更・追加したファイルの一覧（**それ以外を触っていないことの申告**）。
2. `npm test` の**末尾 10 行をそのまま**（`ℹ tests` / `ℹ pass` / `ℹ fail` の行を含む）。件数を文章で言い換えない。
3. `npm run typecheck` と `npm run lint` の終了コード。
4. **D-3 の `sha256sum` の実際の出力**（§4.1 の値と一致すること）。
5. **B-7・B-8・B-8b・B-8c・B-9・B-10 それぞれについて、赤くなった試験名。** 赤くならなかったものはそう書く。
6. **B-1〜B-6 が引き続き赤くなることの確認**（赤くなった試験名）。
7. 反転を戻した後の `sha256sum`。
8. **判断に迷って自分で決めた箇所があれば、その一覧と理由。** 黙って決めない。
