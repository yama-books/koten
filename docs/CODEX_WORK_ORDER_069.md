# 発注069準備書：作者問題の歌を縦書きへ戻し、選択肢を縦書きの札にする

- 作成：2026-09-06、Sol役＝Opus 5。**依頼者の実機確認（公開版 `ce1c596`・64番）から出た。**
- 状態：**裁定済み。ただし着手は発注068の完了後**（同じファイルを触るため。§0.1）。
- 由来：作者問題の画面で、**縦書き設定なのに歌が横書き右揃えで出る**。**選択肢が5つとも墨色べた塗りで、うるさい。**

## 0. 何が起きているか（**原因は特定済み。調査し直さなくてよい**）

### 0.1 歌が横書きになる理由

`styles.css` に**縦書きを打ち消す1行がある。**

```css
.question-text--author .question-poem--author { ...; text-align: start; writing-mode: horizontal-tb; }
```

親（`.question-text--vertical .question-poem`）が `direction: rtl` を持つため、
**打ち消された結果が「横書き・右揃え」として出る。** 実機で見えているのはこれである。

`Session.tsx` 630行目も、作者問題のときだけ**歌をひとかたまりの文字列**として出している。

```tsx
<div class="question-poem question-poem--author" lang="ja">{question.prompt}</div>
```

穴埋めの側は句ごとに `.question-line`（`writing-mode: vertical-rl`）へ割っている。
**作者問題だけがその器に載っていない。**

### 0.2 選択肢がうるさい理由

`Session.tsx` 668行目で、**候補5つすべてに `class="primary"` を付けている。**
`.primary` は `background: var(--color-ink); color: var(--color-paper); font-weight: 700` ——
**主要操作を1つだけ示すための印である。** それが5つ横並びになっている。

### 0.3 これはデザインシステム違反である（**好みの問題ではない**）

`docs/DESIGN_SYSTEM.md` の設計原則より。

- **原則2**：「歌・設問・**選択肢の対象**は縦書き、ボタン・設定・進捗・説明・エラーは横書き。
  **レイアウト都合で本文を横書きにしない。**」→ **歌も選択肢も違反している。**
- **原則1**：「内容を主役にする。**装飾は意味を持つ小さな印に限る。**」→ 墨色べた塗り5つが主役を奪っている。

**発注062 の落ち度である。** 選択肢を選ばせるUIがこのアプリに存在せず、新設したときに
縦書きの器へ載せずに済ませた。**起草者（Sol役）が発注062で「縦書きに載せること」を書かなかったのが原因である。**

## 1. 工程1：作者問題の歌を縦書きに戻す

### 1.1 裁定

- **作者問題でも、歌は設定（縦書き／横書き）に従う。** 作者問題だけの例外を作らない。
- **穴埋めと同じ器に載せる。** 句ごとに `.question-line` へ割り、`.question-text--vertical` の中に置く。
  **新しい縦書きの仕組みを作らない。**
- **句の分割はデータにある。** `poems.json` の各首は `ku: [5句]` を持ち、
  **`Session` は `poems` を props で受け取っている**（`packages/hyakunin/src/ui/screens/Session.tsx` の `poems = []`）。
  `question.poemId` から引いて `ku` を使う。**生成JSONを作り直さない。**
- **`poems` が空のとき（試験など）は、いまと同じく `question.prompt` を1つの列として出す。**
  **落ちないこと。** ここは退避であって、**本番で使われる経路ではない。**
- `.question-text--author .question-poem--author` の **`writing-mode: horizontal-tb` を消す。**

### 1.2 受入条件（工程1）

- 縦書き設定のとき、作者問題の歌が**縦書きで出ること**。**`writing-mode` を DOM から読んで確かめる。**
- **横書き設定のときは横書きで出ること。両方向で確かめる**——片方だけでは切り替えが死んでいても緑になる。
- **句ごとの列が5つ**あること（64番なら「朝ぼらけ／宇治の川霧／たえだえに／あらはれわたる／瀬々の網代木」）。
- `poems` が空でも**例外を投げず**、歌が読めること。
- 穴埋めの画面の見え方が**変わらないこと**（既存の釘が全部緑のまま残ること）。

## 2. 工程2：選択肢を縦書きの札にする

### 2.1 裁定（**依頼者の提案どおり。可能である**）

- **選択肢を縦書きにし、右から左へ並べる。** 位置は歌の下、いまの「作者を選んでください。」の場所でよい。
- **`class="primary"` をやめる。** 候補は互いに対等であり、**主要操作は1つも無い。**
  既定のボタン（`--rule-thin` の枠＋`--color-surface`）にする。
  **選ばれたあとの強調は既存の作法に合わせる**（`aria-pressed` の見せ方＝`--color-focus` の枠と `--color-surface-soft`）。
- **長い名前は2列以上に折り返してよい**（依頼者裁定）。
- **折り返すと横幅が足りなくなる。** 320px・文字200%で、1列は約48px、5列で約272px と収まるが、
  **2列へ折り返すと約520pxとなり収まらない。**
  → **`.question-text--vertical` と同じく、横スクロールできる領域として扱う。**
  **走査からは「名前で」除外する**（059 F-2 の縦書き器と同じ扱い。**ディレクトリや親要素でまとめて外さない**）。
- **「わからない」は横書きのまま、選択肢の外に置く。** これは選択肢ではなく操作である（原則2）。

### 2.2 受入条件（工程2）

- 候補のボタンが `writing-mode: vertical-rl` で描かれ、**右から左へ並ぶこと。**
- **`class="primary"` を持つ候補が0件**であること。**候補が1件以上ある状態で数えること**——
  候補0件の画面で数えても、実装が無くても緑になる。
- **押すと従来どおり採点されること。** `submit(candidate, "choice")` の経路を変えない。
  **既存の作者問題の試験を1本も落とさないこと。**
- 「わからない」が横書きのまま残ること。
- 320px・文字200%で違反0。**作者問題の走査群は既にある**（8件）。
  **横スクロールを許す要素は名前で除外し、除外した根拠を発注書に書くこと。**
  **「通らないから外す」は認めない。**

## 3. 工程3：フィードバック画像の白い背景を消す

### 3.1 調べた事実（**調査し直さなくてよい**）

- 画像は3枚：`assets/feedback/` の `correct-maru`・`needs-review-check`・`perfect-hanamaru`。
- **配信されている `.webp` は3枚とも透明度を持たない**（VP8 lossy、ALPH チャンクなし）。
- **原本の `.png` も3枚とも RGB（colortype 2）で、透明度を持たない。**
  **白は画像の中に焼き込まれている。** 原本から作り直しても、そのままでは透過にならない。
- 白を抜く加工は、**手描きの線の縁に白い縁取りを残す**（アンチエイリアスのため）。**採らない。**

### 3.2 裁定

- **画像を作り直さない。CSS で解決する。** フィードバック画像に **`mix-blend-mode: multiply`** を当てる。
  白は乗算で背景色そのものになるため、**`--color-paper` が何色でも背景と一致する。**
  **色の値を画像に合わせて固定しない**——固定すると、背景色を変えたときに白い箱が戻る。
- **3枚すべてに当てる**（依頼者の「他の画像も同様」）。個別に指定せず、**画像を出す部品の1か所**で当てる。
- **線の色は変えない。** 乗算で紙色と混ざり、わずかに温かくなるのは想定どおりである。

### 3.3 受入条件（工程3）

- 3枚とも、**画像を出す要素に `mix-blend-mode: multiply` が効いていること**（計算後の値を読んで確かめる）。
- **画像そのもの（`assets/feedback/**` と `.webp`）を1バイトも変えないこと。**
  `git diff --numstat -- assets` が **0行**であること。
- 既存の画像に関する釘（`img[src*="correct-maru"]` を見る試験など）が**緑のまま残ること。**

## 4. 工程4：作者の正解表示と「わからない！」

### 4.1 裁定（**依頼者指示・2026-09-06**）

**作者問題の正解表示を、次の形にする。**

```
正解：権中納言定頼　ごんちゅうなごんさだより
（歴史的仮名遣い：ごんちゆうなごんさだより）
```

- **上段は「作者　読み（現代仮名遣い）」。** 区切りは全角空白1つ。
- **下段は歴史的仮名遣いとの異同があるときだけ出す。** 同じなら**行そのものを出さない。**
  **対照するものが無い行は、読み手に差があると誤解させる**（これは既存の作法と同じ理屈である）。
- **`正解：` の接頭辞は残す**（起草者判断）。本番の採点一覧では複数行が並ぶため、
  どれが正解の行かを言葉で示す必要がある。**依頼者が不要と判断したら外してよい。**
- **これは作者問題だけの形である。** 歌本文の表示（`正解：白妙の（しろたへの）` ＋ `△しろたえの（現代仮名遣い）`）は
  **1文字も変えない。** 歌本文は歴史的仮名遣いで答えさせるため、歴史的が主で現代が対照である。
  **作者は選択式で綴りを問うていないため、主従が逆になる。**
- 判定は問題の種別（`type === 'author'` / `skill === 'author'`）で行う。
  **呼び出し側から明示的に渡すこと。** 表示部品が読みの中身から推測しない。

**「わからない」ボタンの表記を「わからない！」にする。**

- **見つけ方が厳密一致の試験が4本ある**（`tests/screen/session.test.tsx`）。**すべて更新すること。**
  **文言だけ変えて試験を直さないと、4本が同時に赤くなる。**

### 4.2 受入条件（工程4）

- 異同がある作者（64番・権中納言定頼）で、**2行とも出ること。**
- **異同が無い作者で、下段が出ないこと。** **両方向で確かめる**——
  片方だけでは「常に出す」実装でも緑になる。**異同の無い作者を正本から1つ選んで fixture にすること。**
- 上段が「作者　読み」の順であること。**読みは現代仮名遣いであること**（歴史的を上段に出さない）。
- **歌本文の表示が変わらないこと。** 既存の釘（`正解：白妙の（しろたへの）`、`△しろたえの（現代仮名遣い）`）が
  **全部緑のまま残ること。**
- 「わからない！」で見つかること。**「わからない」（！なし）で見つかる箇所が0件**であること。
- 本番の採点一覧・紙の自己採点でも、作者問題の行が同じ形で出ること。

## 5. 工程5：マル・チェックを「自分の答え」に重ねる

### 5.1 裁定（**依頼者指示・2026-09-06。実際の丸つけのように見せる**）

- **マル／チェックの画像を、`.answer-retained`（「自分の答え」）の頭に重ねて出す。**
  紙の答案に赤で丸を付けた形に近づける。
- **工程3と組みで効く。** `mix-blend-mode: multiply` で白が消えるため、
  **重ねても四角い白い箱が答えを隠さない。** 工程3を先に済ませること。
- 置き方：`.answer-retained` を `position: relative` にし、画像を `position: absolute` で
  **入力欄の先頭（行頭）に少しはみ出して**重ねる。`pointer-events: none` を付ける
  （**入力欄を押せなくしない**）。

### 5.2 弱めないこと（**ここが本題である**）

- **本番（`exam`）で解答中に正誤を出さない規則を弱めない**（056 F-1・F-2）。
  **開示の段でだけ重ねる。** 解答中に重なる経路を作らない。
- **読み上げの文言を落とさない。** いまの `FeedbackMark` は `label`（「正解」「要確認！」）を持つ。
  **絵だけにしない。** `DESIGN_SYSTEM` の原則5「正誤を色だけで伝えない」は、**位置を変えても効き続ける。**
  重ねたことで文言が視覚的に見えなくなるなら、**読み上げ用の文字として必ず残すこと。**
- **答えの文字が読めなくなるところまで重ねない。** 丸つけは答えを消す行為ではない。

### 5.3 受入条件（工程5）

- 開示の段で、画像が `.answer-retained` に重なっていること
  （**要素の位置を実測して確かめる。CSS の指定があることだけを見ない**）。
- **解答中には重ならないこと。本番でも同じ。両方向で確かめる。**
- 画像に `pointer-events: none` が効き、**入力欄が押せること。**
- 「正解」「要確認！」の文言が、**読み上げに残っていること**（`textContent` か `aria-label` で確かめる）。
- **320px・文字200%で、自分の答えの文字が切れないこと。** 走査で見る。
  **重ねた状態で測ること**——重ねていない状態を測っても何も証明しない。

## 6. 工程6：結果画面の説明が、どちらのボタンのものか分からない

### 6.1 何が起きているか（**原因は特定済み**）

`Result.tsx` 41〜42行目で、**ボタン・説明・ボタンが同じ並びの兄弟**として置かれている。

```tsx
<section class="result-actions" aria-label="次の操作">
  {... && <><button>まちがえた歌だけをもう一度</button><p>まちがえた箇所を確認します。</p></>}
  <button class="primary">同じ範囲をもう一度</button>
</section>
```

`.result-actions` は `display: flex; flex-wrap: wrap` である。
**説明が2つのボタンのあいだに浮き、どちらのものでもなくなる。**
広い画面では横一列に並ぶため、**説明が右隣のボタンの説明に見える。**

**もう1つの欠陥：説明があるのは片方だけである。** 説明の無い側が何をするボタンなのか、
**説明があるせいで、かえって分からなくなっている。**

### 6.2 裁定

- **ボタンと、その説明を1つのまとまりに入れる。** 新しい型を作らない——
  **このリポジトリに既にある `.practice-choice`（ボタンの下に説明を置く型）と同じ作法にする。**
  `styles.css` 343〜348行目に実装がある。
- **両方のボタンに1行ずつ説明を添える。** 片方だけ説明があると、無い側が何なのか分からない。
  **「同じ範囲をもう一度」の説明は「同じ範囲でもう一度出題します。」**とする。
- **読み上げの順序を壊さないこと。** ボタンの直後にその説明が来る形にする。
- 「まちがえた歌だけをもう一度」は**まちがいが無ければ出ない**（現状のまま）。
  **そのとき説明も一緒に消えること。**

### 6.3 受入条件（工程6）

- **説明が、対応するボタンと同じまとまりの中にあること**（DOM の親子で確かめる。見た目の位置で言わない）。
- **2つのボタンが、それぞれ自分の説明を1つ持っていること。**
- **まちがいが0件のとき、「まちがえた歌だけをもう一度」とその説明が両方とも出ないこと。**
  **説明だけが残らないこと。両方向で確かめる。**
- 320px・文字200%で違反0。**結果画面の走査群が無いなら足すこと**（件数の門を付ける）。

## 7. 工程7：本番の設定面で、同じ性質の選択が2つの見た目で出ている

### 7.1 何が起きているか（**原因は特定済み**）

`RangePicker.tsx` の同じ画面に、**排他の2択が2つあるのに、書き方が違う。**

| 設定 | いまの作り | 場所 |
|---|---|---|
| **答え方** | **ボタン2択**（`aria-pressed`。`.answer-mode` の作法） | 91行目〜 |
| **歌の順番**（番号順／ランダム） | **ラジオボタン**（`fieldset` ＋ `input[type="radio"]`） | 67行目〜 |

**同じ性質の設定が、同じ画面で違う見た目になっている。**
加えて、**ラジオの丸は44px に満たず、指で押すと取りこぼす。**

### 7.2 裁定（**依頼者の指摘。切り分けの原則を添える**）

**排他の2択はボタン、独立したON/OFFはチェックボックス。** これを本発注の規準とする。

- **「歌の順番」をボタン2択にする。** `.answer-mode` と**同じ作法に揃える**
  （`aria-pressed`、選択中は `--color-focus` の枠と `--color-surface-soft`）。**新しい型を作らない。**
- **「作者問題」も「あり」「なし」のボタン2択にする**（依頼者の提案・2026-09-06）。
  **チェックボックスは廃す。**
  **経緯を残す**——起草者は当初「独立したON/OFFだからチェックボックスを維持」と推奨した。
  理由は、**1つのボタンにすると、ラベルが「いまの状態」なのか「押すとどうなるか」なのか
  区別できなくなる**ためである（「作者問題も出す」と書かれたボタンは、押すと出るのか、いま出ているのか）。
  **「あり」「なし」の2つを並べる形は、この欠点を持たない**——状態は「どちらが押されているか」で示され、
  ラベルが動作を意味する余地が無い。**依頼者の案のほうが良い。**
  **これで、この画面の3つの設定がすべて同じ作法に揃う。**
- **見出しは「作者問題」とし、ボタンのラベルは「あり」「なし」**。
  **「あり」「なし」だけを置かないこと**——何が「あり」なのかが読み上げで失われる。
  3つとも `.answer-mode` と同じ、見出し＋2ボタンの形にする。

### 7.3 受入条件（工程7）

- **`.range-picker` の中に `input[type="radio"]` が0件**であること。
  **走査対象が空でないことを先に示すこと**——この画面に他の入力が実在する状態で数える。
- 「番号順」「ランダム」が**ボタンで、`aria-pressed` を持ち、常にどちらか一方だけが真**であること。
- **押すと順番が実際に変わること。既存の釘（並び順の試験）を1本も落とさないこと。**
- **`.range-picker` の中に `input[type="checkbox"]` が0件**であること（**走査対象が空でないことを先に示す**）。
- 「あり」「なし」がボタンで、`aria-pressed` を持ち、**常にどちらか一方だけが真**であること。
- **「なし」を選ぶと作者問題が実際に出ないこと。既存の釘を1本も落とさないこと。**
- 3つの設定（答え方・歌の順番・作者問題）が**同じ作法で描かれていること**——
  **見出しと2ボタンの組が3つある**ことを数えて確かめる。
- 320px・文字200%で違反0。**本番の範囲選択の走査群は既にある**（8件）。

## 8. 工程8：範囲の入力が二重になっている

### 8.1 何が起きているか

本番の設定面に「**範囲を確認する**」の見出しと「**60番〜75番**」の表示があり、
**そのすぐ下に、同じ範囲を入れ直す入力欄が2つある**（`RangePicker.tsx` 43・55行目）。
**トップページで選んだ範囲が、同じ画面で2回出ている。**

### 8.2 裁定（**依頼者指示**）

- **既定では、範囲を「文言」だけで見せる**（「60番〜75番」）。**入力欄を最初から出さない。**
- **「変更する」を押したときだけ入力欄を出す。**
- **変更したら、その値が本番に反映されること。** 表示側の見せ方を変えるだけで、
  **範囲の決め方（`normalizeRange` 等）を変えないこと。**

### 8.3 受入条件（工程8）

- **既定の状態で `.range-picker` に `input[type="number"]` が0件**であること。
- **「変更する」を押すと2件出ること。両方向で確かめる**——
  片方だけでは、常に出す実装でも常に隠す実装でも緑になる。
- 変更した値が反映されること（既存の範囲の釘が緑のまま残ること）。
- **320px・文字200%を、閉じた状態と開いた状態の両方で測ること。**
  **開いた状態を測らないと、入力欄について何も証明していない。**
  **本番の範囲選択の走査群を8件から16件へ増やし、件数の門を更新すること。**

## 9. 工程9：「要確認の歌」が何をもって要確認なのか書いていない

### 9.1 定義（**実装から確認済み。調べ直さなくてよい**）

`packages/hyakunin/src/domain/history.ts` の `needsReview()` は、次のものを要確認としている。

- その歌の**最後の解答**（`localDate` が最も新しいもの。**閲覧だけの回は数えない**）が、
- **`incorrect`（まちがえた）**、または **`skipped`（「わからない」を選んだ）** であるとき。

**習熟度の％とは別の基準である。** 直近1回だけを見るため、
**％が高くても、最後の1回をまちがえていれば要確認に入る。**

### 9.2 裁定

- 「要確認の歌」の見出しの下に、**1行で基準を書く。**
  **文言：「最後に解いたとき、まちがえたか「わからない」を選んだ歌です。」**
- **「閲覧だけの回は数えない」までは書かない。** 説明が長くなるほど読まれない。
  **基準の主要部だけを書く。**
- **一覧が空のときの文言（「要確認の歌はありません」）は変えない。**

### 9.3 受入条件（工程9）

- 見出しの直後に、上の文言が**1回だけ**出ること（**厳密一致で数える**）。
- **一覧が空のときも説明が出ること。** 空のときだけ消える作りにしない
  （基準が分からないのは、空のときも同じである）。
- `domain/history.ts` の `needsReview()` を**1行も変えないこと。**
  **これは説明を足す工程であって、判定を変える工程ではない。**
  `git diff --numstat -- packages/hyakunin/src/domain/history.ts` が **0行**であること。

## 10. 変更の境界

**許可**：`packages/hyakunin/src/ui/screens/Session.tsx`、`packages/hyakunin/src/styles.css`、
`packages/hyakunin/src/ui/components/AnswerFeedback.tsx`・`FeedbackMark.tsx`、
`packages/hyakunin/src/ui/screens/Result.tsx`・`RangePicker.tsx`・`History.tsx`、
`tools/overflow-check/index.ts`、対応する試験、`docs/HANDOFF.md`。
**対象外**：出題規則・判定・習熟度・正本・**生成JSON**・台帳・保存スキーマ・統計・送信経路・
**`domain/history.ts` の `needsReview()`**（工程9は説明を足すだけである）・
**`assets/feedback/**` の画像そのもの**（1バイトも変えない）。
**弱めないこと**：056のF群、057の再確認、058の表示、061の送信の門、062〜068の成果。
**とくに**：本番（`exam`）で解答中に正誤を出さない規則、作者問題の配点（本文80%＋作者20%）。

## 11. 破壊試験（12件。**各回で何件赤くなり、何件緑のまま残るかを報告に書くこと**）

1. `writing-mode: horizontal-tb` の打ち消しを1行戻す → **縦書きを見る試験だけ**が赤くなること。
2. 横書き設定でも縦書きで出す → **横書き側を見る試験だけ**が赤くなること。
   **これが赤くならなければ、工程1は片方向しか見ていない。**
3. 候補に `class="primary"` を戻す → **`primary` が0件であることを見る試験だけ**が赤くなること。
4. 句の分割をやめ、歌を1列で出す → **列が5つあることを見る試験だけ**が赤くなること。
5. `mix-blend-mode` を外す → **それを見る試験だけ**が赤くなること。
6. 異同が無い作者でも下段を出す → **下段を出さないことを見る試験だけ**が赤くなること。
   **これが赤くならなければ、fixture の作者に異同があり、片方向しか見ていない。**
7. 上段の読みを歴史的仮名遣いにする → **上段の読みを見る試験だけ**が赤くなること。
8. 解答中にも重ねる → **解答中は重ならないことを見る試験だけ**が赤くなること。
9. 説明をボタンのまとまりの外へ戻す → **親子を見る試験だけ**が赤くなること。
10. 「歌の順番」をラジオへ戻す → **ラジオが0件であることを見る試験だけ**が赤くなること。
    **チェックボックスの陰性試験は緑のまま残るはずである。**残らなければ2つを1本で見ている。
11. 範囲の入力欄を最初から出す → **既定で0件であることを見る試験だけ**が赤くなること。
    **「変更するを押すと出る」側の試験は緑のまま残るはずである。**残らなければ試験が重複している。
12. 一覧が空のときだけ説明を消す → **空のときの説明を見る試験だけ**が赤くなること。
   **見た目の位置しか見ていない試験は、この破壊で赤くならない。**
   **056 の F 群が同時に赤くなるなら、それは別の欠陥を叩いている。切り分けること。**

## 12. Solへ戻す条件（**満たせないときに条件を緩めない**）

- 縦書きの候補を横スクロール可能にしても、320px・文字200%で**縦方向に切れる**と判明した場合。
- `poems` を使うと、`Session` を使う既存の呼び出し（試験を含む）が壊れる場合。
  **`poems` を必須の props に変えないこと。**
- 候補が6つ以上ある首があり、5列を前提にした寸法が破綻する場合。
  **`review/authors.yaml` の候補数を確かめてから設計すること。**

## 13. 検収で繰り返し出ている型（**着手前に読むこと**）

- **報告した以外の検査も回すこと。** 発注066 は `scan:publish` を回さず**公開ゲートが赤のまま**届き、
  画面試験も45件しか回さずに**既存の釘を1件落としたまま**届いた。**`npm test` を全部回し、件数を貼ること。**
- **追加・変更した走査を実際に完走させ、その出力（件数と違反数の行）を報告に貼ること。**
- **破壊試験の結果を報告に含めること。**
- **`check:overflow` は `npm run build` のあとに回し、実行中にビルドし直さないこと。**
- **陰性試験（「0件であること」）は、走査対象が空でないことを先に確かめること。**

## 14. 担当と順序

| 工程 | 担当 | 順序 |
|---|---|---|
| 工程1〜9 | **Terra** | **発注068 の完了後。** 068 と同じ `Session.tsx`・`styles.css` を触るため、並行させない |
| 検収 | **実装しなかった別セッション** | `CODEX_HANDOFF` §2 の V-1 |
| デザイン監査 | **Astra**。**発注068 工程4 とまとめて1回で行う** | 069 の検収合格後 |

**監査を2回に分けない。** 起動費を二重に払うことになるうえ、
**作者問題の画面と初回設定・進捗バーは同じホーム→出題の流れの中にある。** まとめて見たほうが指摘の質が上がる。

## 14.5 補修（2026-09-07・検収の指摘。**条件付き合格の解除条件である**）

**工程5 の起草に漏れがあった。「重ねる」とだけ書き、「元の位置から外す」と書かなかった。**
その結果、**正誤の画像が2か所に出ている**（`AnswerFeedback` の中と、「自分の答え」に重ねたもの）。
**起草者（Sol役）の落ち度である。**

### 14.5.1 P1：正誤の画像を1つにする

- **残すのは「自分の答え」に重ねたほうである**（依頼者の求めは「実際のマルつけのような形」）。
- **`AnswerFeedback` 側は、画像をやめて文言だけにする。**「正解」「要確認！」の文字は残すこと。
  **読み上げから正誤が消えないこと**（`DESIGN_SYSTEM` 原則5）。
- **本番の採点一覧（`.grade-mark`）の画像は別経路である。触らないこと。**
- 受入条件：
  - 開示の段で、**`.answer-feedback` の中に画像が0件**であること。
    **走査対象が空でないことを先に示すこと**（開示の段に達している状態で数える）。
  - **「自分の答え」側には画像が1件あること。**
  - 「正解」「要確認！」の**文言が読み取れること**（`textContent` か `aria-label`）。
  - **画面全体で、正誤の画像が1件だけ**であること。**2か所を別々に数えて終わらせない**——
    片方を消して他方を増やしても、別々に数える試験は両方緑になる。

### 14.5.2 P2：変更した範囲が効くことに釘を打つ

`tests/screen/range-picker.test.tsx` は「既定で出ない」「変更すると2つ出る」を見ているが、
**入れ直した値が実際に使われることを見ていない。** 入力欄を出すところまでで止まっている。

- 受入条件：**「変更する」を押し、`from`・`to` を別の値に入れ、開始したとき、
  `onStart` がその新しい範囲を受け取ること。** 既定値のままでは通らない値にすること。
- **破壊試験**：入力の変更を捨てて既定の範囲で開始するようにする → **この釘だけ**が赤くなること。

### 14.5.3 報告に書くこと

- 上の2件それぞれについて、**追加・変更した試験を完走させた出力**。
- **破壊試験（P1 の画像を2つに戻す／P2 の変更を捨てる）の結果。** 何件赤くなり何件緑のまま残るかまで。
- **釘を足したあと、既存の破壊試験（M-5・M-6 を含む）をもう一度当て直すこと。**
  **それまで1本だけ赤くなっていた行が、2本赤くなっていないか**を確かめる。

## 15. 引き渡し文

**発注068 が完了してから着手してください。** 同じファイルを触ります。

**工程は9つあります。すべて依頼者の実機確認から出たものです。**

| 工程 | 何を直すか |
|---|---|
| 1 | 作者問題の歌が横書き右揃えになっている → 縦書きの器へ載せる |
| 2 | 選択肢5つが墨色べた塗りでうるさい → 縦書きの札にし、`primary` をやめる |
| 3 | フィードバック画像の白い背景 → `mix-blend-mode` で消す（**画像は作り直さない**） |
| 4 | 作者の正解表示の形／「わからない！」 |
| 5 | マル・チェックを「自分の答え」に重ねる（**工程3の後**。白が消えていないと重ねられない） |
| 6 | 結果画面の説明が、どちらのボタンのものか分からない |
| 7 | 本番の設定3つ（答え方・歌の順番・作者問題）を、すべて見出し＋2択ボタンに揃える |
| 8 | 本番の範囲入力が二重 → 文言で見せ、「変更する」で入力欄を開く |
| 9 | 「要確認の歌」の基準を1行で説明する |

**工程3→工程5 の順序だけは入れ替えないでください。** 他は順不同で構いません。

**原因はすべて特定済みです**（各工程の冒頭）。調査からやり直さないでください。
**打ち消しの1行を消し、穴埋めと同じ縦書きの器へ載せる**のが工程1の要点です。
**新しい縦書きの仕組みを作らないでください。**

**選択肢は互いに対等です。** `primary` は主要操作を1つだけ示す印であり、5つ並べるものではありません。

**工程6も同じ考え方です。** 「ボタンの下に説明」の型は `.practice-choice` として既にあります。
**新しい型を作らず、それに揃えてください。**

実装担当記入：未着手。
