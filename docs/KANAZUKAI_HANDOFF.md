# 歴史的仮名遣い確認ツール 引継ぎ

作成日: 2026-08-30
対象: 次のセッション担当者
最終更新: 2026-09-01（第13回セッション）
親文書: `docs/HANDOFF.md`（プロジェクト全体）／ 本書はワークストリーム単位の補足

---

> ## ⚠ このワークストリームは保留中である（裁定 D-21・2026-09-01・依頼者裁定）
>
> **依頼者の指示:**
>
> > 仮名遣いツールについては、百人一首ツールが完成したのちに完成という順序を目指したいため、
> > 共有している部分はそのままでかまいませんが、仮名遣いツール固有の問題については保留として進めます。
>
> **凍結（2026-08-31 の D-09）でも並走でもない、第三の状態である。** 取り違えないこと。
>
> | | 進めてよい | 保留 |
> |---|---|---|
> | 置き場 | `packages/shared/`、`tools/`、`tests/data/`、検査基盤 | `packages/kanazukai/` だけに効くもの |
> | 判定 | **百人一首側も同じ不変条件を必要とする** | 百人一首側には要らない |
> | 例 | 保存スキーマ、習熟度、推薦、デザイントークン、公開ゲート 11 件 | 16 規則、コンバータ、P1c、語彙収集、本ツールの UI、H-12 |
>
> **いま効いていること:**
>
> - **P1b（発注024）は完了・検収済みである**（下記 §1）。**これが仮名遣い側の最後の発注である。**
> - **仮名遣い固有の新規発注はもう出さない。** P1c 以降は百人一首の完成後に再開する。
> - **仮名遣い固有の論点を裁定しない。** 問われても `docs/HANDOFF.md` §4.2 の未決表へ積む。
> - **正本 `仮名遣い規則_一次データ.md` の `reviewStatus: "pending"` は書き換えない。**
> - **「保留」は「新機能を足さない」であって「壊れてよい」ではない。**
>   `npm run typecheck` と 2 製品 `build` は両方を対象にしたまま緩めない。
>
> 詳細は `docs/HANDOFF.md` §8「仮名遣いツールの完成順序」。

## 1. このセッションでやったこと

### 2026-08-30: 設計

設計だけを行った。**実装は一行も書いていない。**

- 仮名遣い変換規則を4資料で照合し、16規則へ整理した
- 設計書を `docs/superpowers/specs/2026-08-30-歴史的仮名遣い確認ツール-design.md` に確定させた
- 裁定 D-06・D-01（`docs/ADR/0004-two-products-layout.md`）へ設計書を適合させた

設計書は依頼者の承認済み。ブレインストーミング工程は完了した。

### 2026-08-31: 発注003の上流工程

`docs/CODEX_WORK_ORDER_003.md` の成果物を作成した。

- `docs/KANAZUKAI_IMPLEMENTATION_PLAN.md` — P0〜P7の実装計画。各フェーズに必須13欄を記載
- `仮名遣い規則_一次データ.md` — 16規則の**人承認待ち案**
- コンバータ受入テスト設計 — 上記実装計画 §7。テストコードはまだ作っていない

規則案の全16行は `proposedBy: "ai"`・`reviewStatus: "pending"` であり、公開・出題には使用できない。ローカル絶対パス、原資料のファイル名・ページ番号が無いことを機械検査した。百人一首の一次資料4点、`古典文法_一次データ索引.md`、`CONSTITUTION.md` はハッシュとmtimeが作業前後で不変だった。

語彙収集は行っていない。設計書 §10.6 どおり、P1cの600件検証と人の裁定が終わるまで開始しない。

### 2026-08-31: 百人一首の先行公開を優先

依頼者の指示により、百人一首の最低限機能での実機公開を本ツールより先に行う。
百人一首側では閲覧に絞った実機確認版が実装されたが、公開阻害要因が残るため、本ツールのP1bには着手しない。
本ツールは規則案・受入テスト設計の人承認待ちを維持し、語彙収集も引き続き禁止する。

### 2026-09-01: 依頼者が 16 規則を承認 → P1b を実装・検収 → **保留へ**

**この日のうちに 3 つのことが起きた。順序を取り違えないこと。**

1. **依頼者が「土台としてこれで問題なし」と承認した**（第12回）。
   ただし「**全体完成後に校正を行う**」という条件つきである。
   したがって正本の `reviewStatus: "pending"` は**そのままにする**。
2. **P1b（コンバータ実装）を発注024 として Terra へ出し、実装された**（第12回〜第13回）。
3. **発注024 を検収し、条件付き合格とした（第13回）。そして D-21 により本ワークストリームは保留に入った。**

#### 発注024 の検収結果（**実装は合格。触らせない**）

親担当が実測した。**Codex の自己申告ではない。**

| 項目 | 実測 |
|---|---|
| `tests/unit/kanazukai/` | **0 → 39 件 pass / 0 fail** |
| `typecheck` / `lint` / `data:check` / 2 製品 `build` | すべて終了コード 0 |
| 一次資料 5 本の SHA-256 | **不変**。`仮名遣い規則_一次データ.md` の `pending` も保たれている |
| 推測ロジック（A-9） | `converter.ts` の `includes` は 2 箇所のみ。**`appliedRules` の重複除去**と**明示文脈 `n1Positions` の位置照合**。文字列からの推測は無い |
| **破壊試験 B-1〜B-10** | **全 10 件が予告どおり名指しで赤くなった。** 復元後のハッシュ一致 |

**適用順（B-6・B-7）が検査できるのは、戻り値に `stages`（中間段階）を含めて深比較しているためである。**
**中間段階を返す設計が、そのまま順序の契約になっている。良い設計なので P1c でも壊さないこと。**

#### 残件 4 件（**いずれも 024 の責任ではない。D-21 により保留**）

| # | 残件 | 再開時にやること |
|---|---|---|
| 1 | **§7.2 の 12 行が 2 本の試験に集約されている**（計画 §12.3 違反）。偽の合格は出ないが、どの行が壊れたか特定しにくい | 試験の分割だけを発注する |
| 2 | **`でうし → ぢようし` は誤り**（正しくは `じょうし`）。**実測で確認済み** | §4 を参照。**試験を同時に足してから直す** |
| 3 | `stages.normalized` が実際には正規化していない（`normalizeForGrading` は §7.3 の指示どおり別関数） | 名前を `input` へ変えるか、別関数を通す |
| 4 | 完了報告が発注書 §7 の 4 項目を欠いていた（対応表・破壊試験・`git status`・A-9 の説明） | 次の発注書で「報告項目を満たさない完了報告は差し戻す」と明記する |

## 2. 読む順序

1. `docs/KANAZUKAI_IMPLEMENTATION_PLAN.md` — 現在の実装順、配置提案、受入テスト設計
2. `仮名遣い規則_一次データ.md` — 16規則の人承認待ち案
3. `docs/superpowers/specs/2026-08-30-歴史的仮名遣い確認ツール-design.md` — 本ツールの確定仕様
4. `docs/ADR/0004-two-products-layout.md` — 裁定 D-06（構成）と D-01（`itemKey`）
5. `docs/HANDOFF.md` §10 — モデル層と資源効率の全体方針。**設計書 §10 はこれに従属する**
6. `CONSTITUTION.md` — 裁定の優先順。競合したらこれが最優先
7. `docs/APP_SPEC.md` §8 ／ `docs/LEARNING_SCIENCE_AUDIT.md` — 習熟度モデルの出所

設計書 §3.2 の規則表と §5 の採点仕様が本ツールの中核である。それ以外は百人一首アプリからの流用が大半を占める。

## 3. 確定していること

| 項目 | 決定 | 出所 |
|---|---|---|
| 配置 | `packages/kanazukai`。公開リポジトリは1つ、Pages の別パスで配信 | 裁定 D-06 |
| 蓄積単位 | **`itemKey = wordId`**。規則は加点対象にせず `Event` の分析次元 | 裁定 D-01 |
| 回答方式 | かな入力＋完全一致採点 | 依頼者 |
| 表示 | 単語モードは**横書き** | 依頼者 |
| 習熟度係数 | `docs/APP_SPEC.md` §8 を流用。ヒント段階を方式の軸に読み替える | 設計書 §6 |
| 例外の扱い | 規則語と分離。v0 に含めない。規則到達度60以上で解禁、出題比率10〜15%上限 | 設計書 §9.5 |
| Codex 層 | Terra が既定、Luna は機械的作業のみ。上流判断へのSol使用は依頼者が個別に許可した場合に限る | `HANDOFF.md` §10.1、発注003 |

## 4. 未決事項

**すべて D-21 により保留である。問われても裁定しない**（`docs/HANDOFF.md` §4.2 の未決表へ積む）。

| ID | 内容 | 期限 | 決定者 | 状態 |
|---|---|---|---|---|
| H-12 | `CONSTITUTION.md` 第7条へ「単語単位の出題は横書きとする」を追記するか | P3 着手前 | 人 | **保留（D-21）。** 憲章の改訂は人が行う |
| ~~H-13~~ | 書き出し形式への `product` 判別子、`schemaVersion` を2へ上げるか | — | — | **2026-08-31 に解消済み。** 依頼者裁定「**足す**」。`schemaVersion` は **1 のまま据え置く**。各要素に必須付与し、欠落・未知の値は「不正な形式」として拒否する。仕様は `docs/APP_SPEC.md` §9.1・§9.2、根拠は ADR-0004 追補。**裁定し直さないこと** |
| — | 長音規則のフィードバック表示（`せ\|う → s\|eu → s\|yō` の子音可視化案と単純併記のどちら） | UI 実装後の実物比較 | 人 | **保留（D-21）。** UI は仮名遣い固有 |
| — | `H1` の語彙的例外の全容 | P1c | Claude Opus 5 xhigh → 人が承認 | **保留（D-21）。** §6 に実測済みの例が 1 件ある |

**H-13 が「未決」としてこの表に残っていたのは記録の不備である**（2026-08-31 に解消していた）。
`docs/HANDOFF.md` §8「検証で見つけて直した誤り」の 6 がまさにこの型の事故を警告している——
**裁定した事項は、決定を書いた場所だけでなく、未決として参照している場所も同時に潰すこと。**
**放置すると、後任がこの表だけを読んで不要に停止する。**

## 5. 次にやること

設計書 §10.6 の段取りに従う。**P1c より前に語を集めない。**

> **⚠ いまやることは「何もしない」である（D-21）。**
> **開始時期は「百人一首ツールが完成した後」。** 2026-08-31 時点の「先行実機確認版の公開後」から、
> **依頼者の第13回裁定でさらに後ろへ動いた。**
> 百人一首側の残りは P6（発注026〜028・検収待ち）→ P7（学習画面）→ P8 以降である。

| 順 | 作業 | 状態 | 次の担当・条件 |
|---|---|---|---|
| 1 | 実装計画の作成 | **完了** | 人が独自提案5点と配置提案を確認 |
| 2 | P0: `仮名遣い規則_一次データ.md` を書く | **完了。2026-09-01 に依頼者が「土台としてこれで問題なし」と承認** | **`reviewStatus: "pending"` はそのまま。全体完成後に校正する** |
| 3 | P1a: コンバータ受入テストの設計 | **完了**（実装計画 §7） | 発注024 の受入条件の正本として使われた |
| 4 | P1b: コンバータ実装 | **完了。2026-09-01 に検収（条件付き合格）** | 発注024。破壊試験 B-1〜B-10 が全件名指しで赤くなった。**残件 4 件は §1 を参照** |
| 5 | P1c: 百人一首600件へ適用し不一致を全件裁定 | **保留（D-21）** | **百人一首ツールの完成後。** 差分抽出は下位へ委譲可、最終裁定は人 |
| 6 | P2 以降: 語の収集 | **開始禁止** | P1cの全件裁定後。候補抽出はLuna lowへ委譲 |

**P1c が唯一の直列区間であり、最も強度を要する**（次段落）。**再開時の最初の仕事はここである。**

**P1c が唯一の直列区間であり、最も強度を要する。** 600件の不一致を「規則表の穴か／百人一首データの誤りか」で1件ずつ裁く作業で、誤ると両プロダクトに誤りが入る。差分抽出と一次分類は Sonnet 5 のサブエージェントへ並列に投げてよいが、**裁定は委託しない**（設計書 §10.5）。

## 6. つまずきやすい点

設計中に実際に間違えた、または間違えかけた箇所。

**発音と表記は別の段である。** 参照した教科書は答えを「コーシ」「チョーズ」とカタカナ長音で示すが、同じページの演習は「現代仮名遣いで答えよ」と要求する。**ツールの正解は `こうし`・`ちょうず` であって `コーシ` ではない。** 完全一致採点なので、ここを混同したデータが1件でも入ると誤採点になる。長音符「ー」を正規化せず誤りとして扱うのは意図的である（設計書 §5.1）。

**`L4`（オ段＋う）は恒等変換である。** 教科書のルール⑤ⓓは発音の規則であり、表記は変わらない（`ようい→ようい`）。一次資料として記録するが出題しない。

**`L1-a` は規則ではない。** 内閣告示第2の「オ列の仮名に『お』を添えて書く」語群（`こおり`・`とお`・`おおかみ`・`ほのお`）は、すべて `H1` と `W2` だけで正しく導出できる。告示がこれを特例としているのは、告示が現代語の音から出発しているためである。独立規則として立てると偽の規則が混入する。

**`H1` には語頭・複合語以外の例外がある。** `はは`（母）は歴史的・現代とも `はは` だが、`H1` を機械適用すると `はわ` になる。一方 `ほほ`（頬）は `ほお` になる。語彙的例外であり、P1c で網羅的に洗い出す必要がある。

> **2026-09-01 に実測で確認した。** 発注024 の実装に `convertHistoricalKana('はは')` を通すと `はわ` を返す。
> **これは欠陥ではなく、設計どおりである**——実装計画 §7.1 が
> 「文脈なしに複合語・助詞・語義・文法形を推測した結果を受入条件にしない」
> 「P1c で見つかる語彙的例外を別 fixture にする」と明記している。
> **語中ハ行を持つ語が百人一首 600 件にどれだけあるかが、P1c の実際の作業量である。**

**濁音のエ段長音（`で`・`ぜ`・`べ` ＋ `う`）に既知の欠陥がある（2026-09-01・実測）。** 発注024 の実装は
`でうし` を **`ぢようし`** と返す。**正しくは `じょうし` である**（対照: `ぜうし → じょうし` は正しい）。

原因は `converter.ts` の `toLongE` が `で: 'ぢよ'` を返すことである。**`ぢ` は現代仮名遣いでは使わない。**
さらに悪いことに、**`D1`（`ぢ→じ`）は長音変換より前に済んでおり、`S1` の文字クラスにも `ぢ` が無い**ため、
**後段の誰もこれを直さない。**

**`で: 'じよ'` の 1 文字で直るが、いま直していない。** 理由は 2 つある。

1. **実装計画 §7 の表に `で`／`ぜ`／`べ` ＋ `う` の例が無く、受入範囲外である。**
2. **試験なしに直すと、無試験の分岐を自分で作ることになる**（百人一首側で 023→025 として塞いだばかりの穴と同じ形）。

**再開時は、§7.1 の表へ濁音のエ段長音を追加してから直すこと。**

**仮名だけでは語が定まらない場合がある。** `くわし` は 菓子（`K1` で `かし`）とも 詳し（歴史的 `くはし`、`H1` で `くわし` のまま）とも読める。`K1` の出題語には制約がある（設計書 §9.3）。

**長音3規則はローマ字でしか正確に書けない。** `せう→しょう` を「エ段＋う は 拗音ょ＋う」と仮名で言い換えると、`せ` が `しょ` になる理由を説明できない。子音が保存され母音だけ変わる規則であり、仮名は子音を単独で表記できないためである（設計書 §3.3）。

## 7. 申し送り

発注003では停止条件S-A〜S-Eに該当する事実は見つからず、設計書の誤りと断定した箇所もない。
実装計画 §9 の**独自提案5点**は、2026-09-01（第12回）に**2 点だけ裁定した**。残りは保留である。

| # | 提案 | 裁定（2026-09-01） |
|---|---|---|
| 1 | 生成工程を `tools/build-data/kanazukai/`、データテストを `tests/data/kanazukai/` に分ける | **未裁定のまま保留。** P1b には不要。**発注024 で「作らない」と明記した**（作れば S-C） |
| 2 | 助詞・複合語・語義・文法形を、コンバータへ**明示的な文脈**として渡す | **採用。** 推測ロジックを書いた時点で不合格とした。**発注024 で実装・検収済み** |
| 3 | **例外保護 → `H1` → 長音規則 → `S1`** の順で適用する | **採用。** 破壊試験 B-6・B-7 で固定した。**発注024 で実装・検収済み** |
| 4 | 600件不一致を実装不備・語彙例外・固有名詞・一次資料異同・規則不足等へ分類する | **P1c で裁定する。保留（D-21）** |
| 5 | 機械的一致の根拠付き一括確認 | **P1c で裁定する。保留（D-21）** |

典拠の重点確認対象は `S1`、`D1-a`、`K1`、`L1`〜`L3`。
**P1b は完了したので、次は提案 4・5 の人確認を得てから P1c へ進む。ただし D-21 により、それは百人一首ツールの完成後である。**

---

## 8. 「保留」の読み違いを防ぐ（**2026-09-01・依頼者の補足指示により追記**）

依頼者の言葉:

> 仮名遣いについては、百人一首アプリが完成したのちに着手の予定でいるため、しばらく保留状態が続いているはずです。

**保留は「長く続く既定の状態」である。** 一時的な足踏みではない。
このワークストリームを開いた人が、次のように読み違えないよう明記する。

| よくある読み違い | 正しい理解 |
|---|---|
| 「保留だから何も触ってはいけない」 | **違う。共有部分は進む。** `packages/shared/`・`tools/`・検査基盤は両プロダクトのものであり、百人一首側の作業で普通に変わる |
| 「保留だから仮名遣い側は壊れていてよい」 | **違う。** `npm run typecheck` と 2 製品 `build` は**両方を対象にしたまま緩めない**。保留は「新機能を足さない」であって「壊れてよい」ではない |
| 「P1b が終わったから P1c へ進んでよい」 | **違う。P1c は保留の対象そのものである。** 再開は百人一首ツールの完成後 |
| 「16 規則の `pending` を承認して先へ進めばよい」 | **違う。** 依頼者の承認は「土台としてこれで進めてよい」であり、「**全体完成後に校正する**」条件つきである。**正本の `reviewStatus` を書き換えない** |
| 「`でうし → ぢようし` は明らかな不具合だから直してよい」 | **違う。1 文字で直るが、いま直さない**（§6）。**試験なしに直すと無試験の分岐を作る。** §7.1 の表へ濁音のエ段長音を足してから直す |
| 「保留中の残件は忘れてよい」 | **違う。** 発注024 の残件 4 件は §1 に列挙してある。**再開時にやる。記録が消えないよう残してある** |
| 「Claude か Codex の判断で保留を解いてよい」 | **違う。解除は依頼者が決める。** D-21 は依頼者裁定である |
| 「保留だから本書はもう更新しなくてよい」 | **違う。** 共有部分の裁定が仮名遣い側に影響する場合（保存スキーマ・習熟度・デザイン）は、**ここへ書き足す**。再開時に読むのはこの文書である |

**保留がいつ解けるかの目安。** 百人一首側に残っているのは
**P6（発注026〜028・検収待ち）→ P7 学習画面 → P8 結果・おすすめ → P9 匿名統計 → P10〜P12（確認ページ・公開前検証・公開移管）**である。
**「完成」の定義は `docs/APP_SPEC.md` §15 の受入条件 12 項目であり、その判断は依頼者が行う。**
最新の進捗は `docs/HANDOFF.md` §1 を見ること。**本書の日付を進捗の目安に使わない。**
