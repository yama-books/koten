# 発注043（P9-A の是正・試験の証明力だけを直す）

- **宛先**: Terra（Codex）
- **起票**: 2026-09-03・第26回・親担当（Claude Opus 5）
- **前提コミット**: `ed3f3a4`（作業ツリー clean）
- **フェーズ**: P9-A の是正。**新機能は無い**
- **規模**: 小

---

## 0. この発注の位置づけ（**先に読むこと**）

### 0.1 何を直すか

**発注042 の実装 4 本は検収に合格した。この発注では実装を 1 行も変えない。**
直すのは **`tests/unit/telemetry/` の 4 ファイルだけ**である。

検収で、親担当が発注書の 10 件とは別に 5 件の破壊試験を回したところ、
**「実装の制約を消しても 367 本が全部緑のままになる」箇所が 3 つ**見つかった。
**実装は正しい。それを守る釘が刺さっていない。**

| # | 見つかった穴 | 実測 |
|---|---|---|
| 1 | `isClientNumber` を `return true` 固定にできる | **367/367 全緑** |
| 2 | 「0 以上の整数」の検査を「数値である」だけに弱められる | **367/367 全緑** |
| 3 | 習熟度の上限 `<= 100` を `<= 0` に潰せる | **367/367 全緑** |

**1 の原因**: `isClientNumber` は正しい値で `true` を返すことしか試されていない。**恒真関数と区別がつかない。**
**2 と 3 の原因**: `fixtures.ts` の `payload()` が**退化している**。
数値項目がほぼ全部 `0` なので、**制約を弱めても、弾かれるべき値がそもそも通らない。**

### 0.2 もう 1 つ直すもの——**P9-B が着手した瞬間に赤くなる検査がある**

W-10〜W-12・W-14 は `packages/shared/src/telemetry/` 配下を**ディレクトリ走査**して禁止語を探す。
**P9-B が `telemetry/transport.ts` を置くと、そこに書くはずの語で W-10 が必ず赤くなる。**

**親担当が実測した**——`transport.ts` に送信の 1 行を書いて置いただけで、W-10 が赤になった。
**これは正しい実装を赤にする検査であり、放置すると P9-B の担当者が 042 の試験を勝手に触ることになる。**

**依頼者裁定（2026-09-03・案 A）により、走査対象をディレクトリではなく名指しの 4 ファイルに寄せる。**
理由は §3 の裁定 1 に書いた。

### 0.3 実測で確かめた事実（**設計の前提。再調査しなくてよい**）

**親担当が `ed3f3a4`・clean で実測した。**

| # | 事実 | 実測値 |
|---|---|---|
| 1 | 着手前の基準線 | `test:node` **367/367/0**、`test:screen` **12 files / 88 passed**、`scan:publish` **751 件 / 違反 0** |
| 2 | `typecheck` `lint` `data:check` `build` `scan:publish` | **すべて終了コード 0** |
| 3 | `telemetrySources()` の現在の番人は**件数 4 件**での判定である | `client-number.ts` を消して別の `.ts` を 1 つ置くと、**W-13 は緑のままだった**（実測） |
| 4 | `telemetry/` 配下の `.ts` は**現在ちょうど 4 件** | `client-number.ts` `queue.ts` `registry.ts` `sanitize.ts` |
| 5 | 発注042 の C-5 は W-15 を**単独では分離できない** | `createClientNumber` を固定値にすると **W-15 と W-18 の 2 本**が赤くなる。**これは既知の事実として受け入れる。この発注では直さない** |

### 0.4 変更してはならないファイルの SHA-256（`sha256sum -c` に流せる）

```
4026c77a2c36bbc0ec4568e409227039701da54c7d550f3df0db0e3eb3d8fbe2 *packages/shared/src/telemetry/client-number.ts
bfd407bbf800dfec761cbdc1771f47f1ab7c299f58da6a61f465b876550feb20 *packages/shared/src/telemetry/queue.ts
bb6014014d6960c0da2ade7c1e27bec77efcb3c8630ff02263d569b09179e8b7 *packages/shared/src/telemetry/registry.ts
73891ee555ce19ca540bf6cb87508ff3c5ccabd738fd93e4da9e3d6344f1faf7 *packages/shared/src/telemetry/sanitize.ts
207ac60c33d467651919ffd6549ba062cc12b2485d59be44862f558efca017e0 *packages/shared/src/domain/event.ts
9c264f7b884fad23f4b47e1912d1a7cbba23c1a434aa137d1a43604f4ff006c4 *packages/shared/src/storage/repo/outbox.ts
936a87717f33872c34a22c64b9a1e08b7a6f9c37391bd9eb80b9a493f27bd5fa *packages/shared/src/storage/repo/settings.ts
038d0588fd71f056a6065a870205bfe1587f1ed6cd7d106a3e0e5789f37c9a3a *packages/shared/src/storage/reset.ts
69c1228a3013669baee2230c1268feaeefa0b8ea61bbd4b18e194f13e9b00697 *packages/shared/src/storage/import.ts
e84602e0e5026b8747fcf311a4b949a28079c87b943f3f3a7f4034471590a94b *packages/shared/src/storage/export.ts
cdd20f59d50477ace6971fb9a081767998b11852438ef3ca7866c1a984128aba *packages/shared/src/app-config.ts
65436b90add6455b73ff5ca2897716fd68e8fbfaa7db8d71909d653f4d2ca2ff *packages/shared/package.json
66abc9e04ae15b7a64463da6aeded1b687c097d0a80e5ff09c5e64807cbd278a *packages/shared/src/storage/schema.ts
```

**この 13 件が 1 バイトでも動いたら、それは変更境界の逸脱である。**
**とくに先頭 4 件——042 の実装そのもの——を直したくなったら、それは S-2 である。**

---

## 1. 変更境界

### 変更してよいファイル（**4 つだけ**）

```
tests/unit/telemetry/fixtures.ts
tests/unit/telemetry/registry.test.ts
tests/unit/telemetry/client-number.test.ts
tests/unit/telemetry/static.test.ts
```

### 新規作成してよいファイル

**無い。1 つも無い。**

`tests/unit/telemetry/queue.test.ts` も**変えない**（この発注は queue と sanitize に触らない）。

### 絶対に変更しない・作らないもの

| 対象 | 理由 |
|---|---|
| §0.4 の 13 ファイル | **実装は検収に合格している。試験の穴を実装で埋めない** |
| `packages/` 配下すべて | **この発注は `packages/` を 1 バイトも動かさない**（A-8） |
| `telemetry/transport.ts` | **P9-B の範囲。C-2 の破壊試験で一時的に作るが、必ず消す** |
| `firebase/` 配下 | **P9-B の範囲** |
| `docs/**` | **この発注書を含め、文書は 1 文字も書き換えない**（訂正は親担当が §8 に足す） |
| 既存の W-1 〜 W-28 の**名前とアサーション** | **消さない・書き換えない。足すだけである**（下の裁定 4） |

---

## 2. 先に読むもの

| 資料 | 見る箇所 |
|---|---|
| `docs/CODEX_WORK_ORDER_042.md` | **§3 の裁定 2**（17 キーとその制約）、**§4.5 の W-18 の注記**、**§5.1**（破壊試験の考え方） |
| `docs/HANDOFF.md` | **§4.1 の D-58**（送信フィールド）、**D-60**（この発注の裁定 1） |
| `tests/unit/telemetry/` の 5 ファイル | 現状。**とくに `fixtures.ts` の `payload()` が退化していることを自分の目で見ること** |

---

## 3. 裁定済み事項（**これに従う。決め直さない**）

### 裁定 1: 禁止語の検査対象は、ディレクトリではなく名指しの 4 ファイルにする（依頼者裁定・案 A・D-60）

`fixtures.ts` に定数を置き、`telemetrySources()` はその名前で読む。

```
TELEMETRY_FILES = ['client-number.ts', 'queue.ts', 'registry.ts', 'sanitize.ts']
```

**理由**: 発注042 の裁定 1（ネットワークに触れない）は、**「`telemetry/` というディレクトリ」ではなく「あの 4 モジュール」に掛かった約束である。**
ディレクトリに掛けると、P9-B が同じ部屋に送信の実装を置いた瞬間に**正しい実装を赤にする。**

**副作用として、件数による番人が要らなくなる。**
名前で `readFileSync` するので、**1 つでも欠ければその場で例外になる**（現在の「4 件あれば素通り」より強い）。
**§0.3 の 3 の穴はこれで塞がる。**

### 裁定 2: 名指しにした代わりに、「増えたこと」を見る検査を 1 本足す

名指しにすると、**5 本目の純関数モジュールが黙って検査の外に置かれる**恐れがある。
そこで **X-9** を置く——`telemetry/` 配下の `.ts` が、
**検査対象（`TELEMETRY_FILES`）か、試験の中に書いた免除一覧のどちらかに載っていること**を確かめる。

**免除一覧は空で始める。P9-B が `transport.ts` をそこへ 1 行足す。**
**これは手間ではなく、意図した関門である**——**黙って検査の外に出る**のと、
**一覧に名前を書いて外に出す**のとでは、後から読んだときの意味がまるで違う。

### 裁定 3: `fixtures.ts` の `payload()` を非退化にする

**数値を「制約に触れる値」にする。** 親担当が参照実装で使った値を挙げる。**これをそのまま使ってよい。**

| キー | 値 | 何を撃つ値か |
|---|---|---|
| `clientNumber` | `k3m9qz7x2w5b8n4v6t1r` | 英字だけでなく**数字も含む**（`[a-z0-9]` の両方） |
| `grade` | 空文字（**変えない**） | 裁定 2 の「空文字を許す（未選択）」を守る固定ピン |
| `pageViews` | `7` | 0 でない |
| `buttonCounts` | `{ start: 3, answer: 12, hint: 1, reveal: 4, history: 2, report: 0 }` | **`report: 0` を 1 つ残す**（0 が許されることも守る） |
| `entryCounts` | `{ quick: 5, view: 1, learn: 8, review: 2, exam: 0 }` | 同上 |
| `questionTypeCounts` | `{ blank: 9, author: 6 }` | — |
| `attemptCount` | `15` | — |
| `masteryAvg` | `62.5` | **整数でない。**「平均は整数でなくてよい」を守る |
| `masteryMax` | `100` | **上限そのもの。** 上限を潰す変更がここで死ぬ |
| `masteryDistribution` | `[4, 3, 2, 1, 5]` | **5 個が互いに異なる。** 並べ替えの誤りが見える |
| `dataVersion` | `3` | — |
| `masteryRulesVersion` | `2` | — |

**`localDate` `product` `isOfficial` `appVersion` `expiresAt` は変えない。**

**この値にすると、既存の W-4・W-5・W-28 が弁別的になる**（§5.1 の C-5・C-7 が実証する）。

### 裁定 4: 既存の W-1 〜 W-28 を書き換えない。足すだけである

**名前も、アサーションも、種別も動かさない。**
042 の破壊試験 C-0〜C-9 の期待値がその名前で書いてあり、**書き換えると検収の土台が消える。**
`fixtures.ts` の変更で W-4・W-5・W-28 の**証明力が上がる**のは構わない。**文面を触るなという意味である。**

### 裁定 5: 新規試験の名前は `X-` で始める

042 が `W-` を使ったので次の記号にする。`X-1` から `X-9` まで。
**§4.2 の表に書いた「種別」を試験名のコメントに書くこと**（042 と同じ形式）。

### 裁定 6: `registry.ts` の制約は正しい。実装で埋めない

X-1 〜 X-6 が赤くなったら、**それは試験の書き方が違うということである。**
**`isStatsPayload` や `isClientNumber` に手を入れて緑にしてはならない**（S-2）。

---

## 4. 実装範囲

### 4.1 `tests/unit/telemetry/fixtures.ts`（変更）

1. **`TELEMETRY_FILES` を export する**（裁定 1 の 4 つ）。
2. **`telemetrySources()` を名指し読みに変える。** 件数の番人（4 件未満で投げる行）は**消してよい**——名前で読めば欠けた時点で投げる。
3. **`telemetryDirectoryFiles(): string[]` を新設する。** `telemetry/` 配下の `.ts` の**ファイル名だけ**を返す（X-9 が使う）。
4. **`payload()` を裁定 3 の値にする。**

**`payload()` の戻り値の型・キーの順序・キーの数は変えない。値だけを変える。**

### 4.2 新規試験（**9 本ちょうど**）

**新しい試験ファイルを作らない。既存の 3 ファイルの末尾に足す。**

| # | 置き場所 | 名前 | 見るもの | 種別 |
|---|---|---|---|---|
| X-1 | `client-number.test.ts` | `client-number: 長さが 20 でない文字列を弾く` | 空文字・19 文字・21 文字が `false` | 弁別的 |
| X-2 | `client-number.test.ts` | `client-number: [a-z0-9] 以外を含む値と文字列でない値を弾く` | 大文字・記号・`undefined`・`null`・数値・配列・`{ length: 20 }` が `false` | 弁別的 |
| X-3 | `registry.test.ts` | `registry: 負の計数を弾く` | `pageViews` `attemptCount` `buttonCounts.start` `masteryDistribution` の末尾 `dataVersion` に `-1` | 弁別的 |
| X-4 | `registry.test.ts` | `registry: 整数でない計数を弾く` | 上のうち 4 箇所に `0.5` / `1.5` / `2.5` | 弁別的 |
| X-5 | `registry.test.ts` | `registry: 習熟度は 0 以上 100 以下である` | **`100` と `0` は通る。`101` と `-1` は弾く**（avg と max の両方） | 弁別的 |
| X-6 | `registry.test.ts` | `registry: 習熟度の平均は整数でなくてよい` | `62.5` が `true` | **固定ピン** |
| X-7 | `registry.test.ts` | `fixtures: 標準 payload は境界と非整数を含んでいる` | `masteryMax` が `100`、`masteryAvg` が非整数、`masteryDistribution` の 5 個が互いに異なる | **固定ピン** |
| X-8 | `static.test.ts` | `static: 禁止語の検査対象が名指しの 4 ファイルに固定されている` | `telemetrySources()` の返す名前が**ちょうどあの 4 つ** | **固定ピン** |
| X-9 | `static.test.ts` | `static: 検査対象にも免除一覧にも無い実装が増えていない` | ディレクトリの `.ts` が全部、検査対象か免除一覧に載っている | 弁別的 |

**X-5 は fixture に依存させないこと。** `100` も `-1` も**試験の中で明示的に置く**。
**理由**: fixture が将来また退化しても X-5 は効き続ける必要がある。
**fixture の非退化そのものは X-7 が単独で守る**（§5.1 の C-9）。

**X-8 は 4 つの名前を試験の中に書き写してよい。** 042 の裁定 3（一覧を 2 箇所に書かない）とは**目的が違う**——
X-8 の役目は「**この一覧が勝手に増えたり減ったりしないこと**」であり、
`fixtures.ts` の定数と読み比べたら**定数を定数と比べるだけの空の試験になる。**

**X-9 の免除一覧は、試験の中のローカル変数でよい。空配列で始める。**
**`fixtures.ts` に置かないこと**——免除は「この検査の判断」であって、道具の設定ではない。

---

## 5. 受入条件（**すべて機械判定できること**）

| # | 条件 |
|---|---|
| **A-1** | `npm run test:node` が **tests 376 / pass 376 / fail 0**（着手前 367 ＋ 新規 9。**親担当が参照実装で実測した値**） |
| **A-2** | `npm run test:screen` が **12 files / 88 passed** のままである |
| **A-3** | `typecheck` `lint` `data:check` `build` `scan:publish` がすべて終了コード 0。`scan:publish` は **751 件 / 違反 0** |
| **A-4** | §0.4 の 13 ハッシュが **13/13 一致**する |
| **A-5** | `git status --porcelain -uall` に **`??` の行が 1 つも無い**（新規ファイルを作っていない） |
| **A-6** | 変更されたファイルが §1 の **4 つだけ**である |
| **A-7** | `TELEMETRY_FILES` の要素数が **ちょうど 4** である |
| **A-8** | `git diff --name-only` に **`packages/` で始まる行が 1 つも無い** |
| **A-9** | 新規試験が **X-1 〜 X-9 のちょうど 9 本**である |

### 5.0 grep で判定する条件

- **g**: `git diff --name-only` に `packages/` → **0 件**（A-8 と同じものを別の形で見る）
- **h**: `git diff --name-only` に `tests/unit/telemetry/queue.test.ts` → **0 件**（この発注は queue に触らない）
- **i**: `tests/unit/telemetry/` に `W-` で始まる試験名が **28 本**残っている（裁定 4。1 本も消えていない）

### 5.1 破壊試験（**受入の中心。ここを通らなければ緑でも合格にしない**）

**下の期待値は、親担当が参照実装を当てて 10 件すべて実際に走らせた実測値である**（推論ではない）。
**Terra は 10 件を自分で再現し、赤くなった試験の名前を完了報告に書くこと。**
**壊したファイルは必ず `cp` で復元し、復元後にハッシュが一致することを確かめる**
（Python での書き戻しは改行が変わることがある）。

| # | 壊すもの | 赤くなる試験（実測） | 件数 | 既存 339 件 |
|---|---|---|---|---|
| **C-0** | 実装 4 本を消す | 3 ファイルがファイルごと失敗＋**W-10・11・12・13・14・X-8・X-9** | 10 | 全緑 |
| **C-1** | **`client-number.ts` だけ**を消す | `client-number.test.ts` がファイルごと失敗＋**W-9・W-10・11・12・13・14・X-8・X-9** | 9 | 全緑 |
| **C-2** | `telemetry/transport.ts` を送信の 1 行つきで**足す** | **X-9 だけ**（**W-10 は緑のまま**。ここが裁定 1 の心臓） | 1 | 全緑 |
| **C-3** | `isClientNumber` を `return true` 固定 | **X-1・X-2** | 2 | 全緑 |
| **C-4** | 「0 以上の整数」の検査を「数値である」だけに弱める | **X-3・X-4** | 2 | 全緑 |
| **C-5** | 習熟度の上限 `<= 100` を `<= 0` に潰す | **W-4・W-5・W-28・X-5・X-6** | 5 | 全緑 |
| **C-6** | 習熟度の下限 `>= 0` を無効にする | **X-5 だけ** | 1 | 全緑 |
| **C-7** | `masteryAvg` に整数条件を**足す**（過剰制約） | **W-4・W-5・W-28・X-6** | 4 | 全緑 |
| **C-8** | `TELEMETRY_FILES` から `client-number.ts` を**外す**（検査自身を壊す） | **X-8・X-9** | 2 | 全緑 |
| **C-9** | `payload()` の数値を全部 `0` に戻す（**退化させる**） | **X-7 だけ** | 1 | 全緑 |

**この表の読み方。**

- **C-2 が 1 本だけで、しかも W-10 が緑であること**が、この発注の目的そのものである。
  **W-10 が赤くなったら、走査がまだディレクトリに掛かっている。**
- **C-3・C-4・C-6・C-9 は「着手前は 0 本だった」**ものである。
  **親担当が検収時に実測した**——同じ 4 つの破壊を `ed3f3a4` に当てると、**いずれも 367/367 全緑だった。**
  **この 4 件が赤くなることが、この発注が仕事をした唯一の証拠である。**
- **C-5 と C-7 は逆向きである**（制約を緩める／きつくする）。**重なるのは W-4・W-5・W-28・X-6。**
  **X-5 は緩める側だけ、X-7 は fixture だけを見る。** 3 本の役目が分かれている。
- **C-8 は検査自身の破壊である。** 検査道具が壊れたときに黙らないことを見る。
- **C-9 は「fixture が退化して元に戻る」未来を撃つ。** X-7 が単独で赤くなる。

**C-2 で W-10 が赤くなった、または C-3・C-4・C-6・C-9 のいずれかが緑のままだった場合は、
名前を報告に書いて止まること（S-4）。**

### 5.2 試験の質

**完了報告に、新規 9 本それぞれについて「種別」と「どの破壊試験で赤くなったか」を書くこと。**
**§4.2 の表に親担当の判定を書いてあるので、食い違ったらそれを報告すること。**
**食い違い自体は減点ではない。黙って合わせるのが減点である。**

| 裁定 | 守る試験 |
|---|---|
| 裁定 1（名指しの 4 ファイル） | X-8（C-2・C-8） |
| 裁定 2（増えたことを見る） | X-9（C-2） |
| 裁定 3（fixture の非退化） | X-7（C-9）、および W-4・W-5・W-28 の証明力向上（C-5・C-7） |
| 裁定 4（既存を書き換えない） | §5.0 の i |
| 042 の裁定 2（0 以上の整数・0〜100） | X-3・X-4・X-5・X-6 |
| 042 の裁定 5（利用番号の字種と桁数） | X-1・X-2 |

---

## 6. 停止条件（**該当したら手を止めて親担当へ報告する**）

| # | 条件 |
|---|---|
| **S-1** | §0.4 の 13 ハッシュが**着手前に**一致しない（誰かが並行して触っている） |
| **S-2** | **`packages/` を変えないと X-1 〜 X-9 を緑にできないと判断した。** 実装は検収に合格している。**試験の穴を実装で埋めない** |
| **S-3** | §1 の 4 ファイル以外を変える／作る必要が出た |
| **S-4** | **C-2 で W-10 が赤くなった**、または **C-3・C-4・C-6・C-9 のいずれかが緑のまま**だった |
| **S-5** | 既存の W-1 〜 W-28 のどれかを書き換えないと通らないと判断した（裁定 4 と衝突する） |
| **S-6** | 受入条件どうしが矛盾する、または本発注書の記述に誤りを見つけた。**自分で解釈して進めない** |

---

## 7. 完了報告に必ず書くこと

1. 全ゲートの**実測値**（A-1〜A-3 の数字をそのまま）
2. §0.4 の 13 ハッシュの照合結果
3. `git status --porcelain -uall` の全文と `git diff --name-only` の全文
4. §5.0 の grep **g・h・i** の実測件数
5. **破壊試験 C-0〜C-9 の 10 件それぞれについて、赤くなった試験の名前**（§5.1 の実測値と食い違ったらそれも書く）
6. **新規試験 9 本それぞれの種別（弁別的／固定ピン）**と、**どの破壊試験で赤くなったか**
7. **独自に決めたこと**（**無ければ「無し」と書く**）
8. **できなかったこと・迷ったこと**（**無ければ「無し」と書く**）

**7 と 8 は検収側で再現できない。** 空欄で出さないこと。

---

## 8. 訂正

（走行中に親担当が追記する。**Terra はこの節を書き換えない。**）
