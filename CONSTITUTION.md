# 古典学習帳 監理憲章

最終更新: 2026-08-30

対象: アプリ本体、学習データ、紹介・使い方・変更履歴、公開リポジトリ、管理画面、統計、開発用確認ツール

目的: 中学生・高校生が試験範囲の古典を自分で確認できる、簡潔で信頼できる学習道具を公開する。

## 1. 裁定の優先順

判断が競合した場合は、次の順で決める。

1. 校正済み本文と根拠資料を損なわない
2. 学習者の記録を失わせない
3. 学習上の有効性と表示上の誠実さを保つ
4. 中高生が説明なしでも始められるようにする
5. アクセシビリティとスマホでの使いやすさを保つ
6. 実装・画面・README・使い方の説明を一致させる
7. 個人を特定しない範囲で、指導上必要な統計を正確に得る
8. 初回公開を遅らせる機能は段階実装へ回す
9. 検証の信頼性を保ちながら、実行時間と計算資源を必要以上に使わない

大きな仕様変更は、目的・影響・代替案を示し、裁定を経てから実装する。

## 2. 学習上の境界

- 目的は自主学習であり、学校の成績処理には利用しない。
- 習熟度％は学習履歴から求める内部指標であり、能力・評定・記憶確率とは表示しない。
- 順位、偏差値、他者比較、連続利用を途切れさせないための圧力を設けない。
- 見るだけでも学習の入口として扱うが、閲覧だけで高い習熟度に到達させない。
- 想起、正答、間隔を空けた再確認を重く扱う。
- 不正解は罰ではなく、次に確認する対象を決める証拠として扱う。
- 「おすすめ」は一度に一件とし、利用者は終了や別の範囲を選べる。

## 3. 一次資料と生成物

- `百人一首_本文・作者_一次データ.md`を本文・作者の正本とする。
- 読みのMarkdown、異同記録、文法索引を用途別の正本とする。
- 正本は変換処理、問題生成、表示都合で書き換えない。
- JSON、索引、穴埋め問題、候補一覧は再生成可能な派生物とする。
- 句切れ、穴埋め候補、作者別名、助動詞判定は、人による確認状態と根拠を持つ。
- 複数解釈がある句切れは、問題の正解として断定せず、表示上の便宜として採用した一案であることをデータ上区別する。
- 生成AIを正本の確定、公開時の問題生成、正解判定の根拠にしない。

## 4. 公開範囲

「公開」は公開URLだけでなく、公開リポジトリ、Git履歴、raw URL、配布物を含む。

公開してよいもの：

- Webアプリ、紹介、使い方、変更履歴
- 公開に必要なデータ、画像、フォント、ライセンス、設定
- 秘密情報や実利用データを含まないソースとテスト

公開しないもの：

- 原資料PDFのうち再配布許可を確認していないもの
- 管理者認証情報、API秘密鍵、サービスアカウント、トークン
- 実利用者データ、未集計の報告、バックアップ
- ローカル管理ツール、内部の発注文書、作業メモ、引継ぎ情報
- 未確認の文法判定や公開前レビュー結果

リンクを置かないことや推測しにくいURLは、非公開の根拠にしない。生徒に見せない管理画面は、公開ビルドから除外するか、サーバー側で管理者認証を行う。

## 5. 保存と端末間共有

- 初回公開は端末内保存を基本とする。
- 回答・閲覧・自己評価ごとに保存し、通信失敗で履歴を失わせない。
- 履歴の読み込みは既存記録を消さず、重複を除いて統合する。
- 初期化は端末内の習熟度・回答・閲覧履歴を対象とし、表示設定は残す。
- 初期公開では書き出しファイルまたは貼り付け文字列で端末間移行する。
- クラウド同期、QR共有、任意ログインは初回公開後に追加する。

## 6. 匿名統計

指導上の参考として、匿名利用番号数、学年区分、利用回数、機能別回数、習熟度の集計値を取得する。匿名利用番号数は実人数ではないことを管理画面で明示する。

- 氏名・学校名・連絡先の入力欄を設けない。
- 学年は「中一・中二・中三・その他」とし、「その他」は小学生・高一・高二・高三・大人に分ける。
- 同じブラウザ保存領域で継続する無作為な利用番号を使う。この番号は氏名や端末情報から作らず、端末固有ID、広告ID、フィンガープリント、正確な時刻を送らない。番号数は実人数ではなく利用規模の目安として扱う。
- 画面文言、入力回答、手書き内容、学習履歴そのものを統計へ送らない。
- テスト運用と正式公開をデータ上で分離し、正式公開日時以後だけを正式統計とする。
- 生徒には自分の履歴だけを示し、全体・学年別集計は認証済み管理画面だけで示す。
- 統計送信の失敗は学習を妨げず、通常画面へ警告を出さない。

利用者向け説明は次の文を基本とする。

> 利用状況を一部集計します。集計には無作為な番号を使用するため、個人が特定されることはありません。

## 7. UIと文体

- 本文と回答対象は縦書き、操作・設定・進捗は横書きとする。
- ホームでは「とりあえず始める」を第一操作にする。
- 主要画面で同時に提示する判断を減らし、詳細設定は開始後または別画面へ置く。
- 和紙、桜、金箔などの記号を過剰に使わない。
- 抽象的な効能より、利用者が画面で行うことを書く。
- 正誤・習熟度は色だけで伝えず、％、記号、文言を併用する。
- 操作名と説明文で同じ語を使う。
- 安全な操作には確認ダイアログを挟まない。削除・統合など影響のある操作だけ、対象を示して確認する。

## 8. アクセシビリティ

- 320 / 375 / 414 / 768pxで横あふれ、文字切れ、操作不能がないことを確認する。
- iOS Safari、Android Chrome、iPad Safariを初回公開の必須確認対象とする。
- タップ領域は原則44px以上とする。
- キーボード操作、可視フォーカス、200%拡大、読み上げ順序を確認する。
- 縦書き表示とは別に、読み上げ用の自然なテキスト順序を持つ。
- ドラッグ、音声、手書き認識を必須操作にしない。
- `prefers-reduced-motion`を尊重し、効果音は初期状態で使用しない。

## 9. 検証

テストは変更による失敗可能性に合わせて選ぶ。

1. 変更箇所の静的検査
2. 影響する画面・代表幅の確認
3. 保存形式、同期、削除、統計、安全性に触れた場合だけ関連回帰テスト
4. 全テストと実機一巡は公開直前に実行

文言・色・余白だけの変更に、無関係なデータ変換・同期・全機能テストを毎回実行しない。反対に、正本変換、習熟度計算、履歴統合、Firestoreルールは自動テストなしで変更しない。

## 10. 公開判定

次を満たさない版は正式公開しない。

- 100首の番号、本文、作者、読みの参照整合が取れている
- 正本と生成JSONの不一致を検出できる
- 範囲URL、学習履歴、統合、初期化が代表環境で動く
- 縦書きが代表幅で読め、横書き切替が機能する
- 匿名統計と正式・テスト区分が検証できる
- 公開物に秘密情報、内部資料、許可未確認の原資料がない
- README、仕様、使い方、画面文言が一致する
- 既知の制約を「完全対応」などの表現で隠していない

## 11. 再評価

本文正本、保存形式、同期、統計、正解判定、習熟度計算を変更するときは、本憲章への影響を確認する。公開説明の誤りは新機能より先に直す。本憲章は半年ごと、または上記の仕組みを変えたときに再評価する。
